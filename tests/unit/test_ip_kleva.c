/* AUTO-GENERATED by kleva (KLEE + EVA) — DO NOT EDIT
 * Oracle values are EVA-proven singletons — not hand-written expected values.
 * Re-generate: kleva gen <module.yaml>
 * Generated: 2026-06-17 10:26:18
 */
#include <assert.h>
#include <stdio.h>
#include <string.h>
#include <stdint.h>
#include "ip.h"

#include "ip.h"
static int kleva_stub_IpProtocolHandler(Interface *iface, Packet *pkt, void *ctx) {
    (void)iface;
    (void)pkt;
    (void)ctx;
    return 0;
}

static void test_init_null_tv001(void) {
    ip_init(NULL, NULL);
    int out_ok = 1;
    assert(out_ok == 1); /* EVA-proven oracle */
    printf("PASS test_init_null_tv001\n");
}

static void test_init_registers_handlers_tv001(void) {
    Topology *sim_topo = topology_create();
    assert(sim_topo != NULL);
    Scheduler *sim_sched = scheduler_create(64);
    assert(sim_sched != NULL);
    Simulator *sim = simulator_create(sim_topo, sim_sched);
    assert(sim != NULL);
    IpStack stack;
    memset(&stack, 0, sizeof(stack));
    ip_init(sim, &stack);
    int out_ok = 1;
    assert(out_ok == 1); /* EVA-proven oracle */
    simulator_free(sim);
    printf("PASS test_init_registers_handlers_tv001\n");
}

static void test_stack_init_null_tv001(void) {
    Topology *sim_topo = topology_create();
    assert(sim_topo != NULL);
    Scheduler *sim_sched = scheduler_create(64);
    assert(sim_sched != NULL);
    Simulator *sim = simulator_create(sim_topo, sim_sched);
    assert(sim != NULL);
    ip_stack_init(NULL, sim);
    int out_ok = 1;
    assert(out_ok == 1); /* EVA-proven oracle */
    simulator_free(sim);
    printf("PASS test_stack_init_null_tv001\n");
}

static void test_stack_init_valid_tv001(void) {
    IpStack stack;
    memset(&stack, 0, sizeof(stack));
    ip_stack_init(&stack, NULL);
    int out_ok = 1;
    assert(out_ok == 1); /* EVA-proven oracle */
    printf("PASS test_stack_init_valid_tv001\n");
}

static void test_stack_bind_interface_null_tv001(void) {
    int out_ret = ip_stack_bind_interface(NULL, NULL);
    int out_sentinel = (out_ret == -1) ? 1 : 0;
    assert(out_ret == -1); /* EVA-proven oracle */
    assert(out_sentinel == 1); /* EVA-proven oracle */
    printf("PASS test_stack_bind_interface_null_tv001\n");
}

static void test_stack_bind_interface_valid_tv001(void) {
    IpStack stack;
    memset(&stack, 0, sizeof(stack));
    uint8_t iface_mac[6] = {0, 0, 0, 0, 0, 0};
    Interface *iface = interface_create("kleva", iface_mac, 0x0100A8C0, 24, 1500);
    assert(iface != NULL);
    int out_ret = ip_stack_bind_interface(&stack, iface);
    int out_sentinel = (out_ret == 0) ? 1 : 0;
    assert(out_ret == 0); /* EVA-proven oracle */
    assert(out_sentinel == 1); /* EVA-proven oracle */
    interface_free(iface);
    printf("PASS test_stack_bind_interface_valid_tv001\n");
}

static void test_stack_register_protocol_null_tv001(void) {
    int out_ret = ip_stack_register_protocol(NULL, 0, NULL, NULL);
    int out_sentinel = (out_ret == -1) ? 1 : 0;
    assert(out_ret == -1); /* EVA-proven oracle */
    assert(out_sentinel == 1); /* EVA-proven oracle */
    printf("PASS test_stack_register_protocol_null_tv001\n");
}

static void test_stack_register_protocol_valid_tv001(void) {
    IpStack stack;
    memset(&stack, 0, sizeof(stack));
    int out_ret = ip_stack_register_protocol(&stack, 0, kleva_stub_IpProtocolHandler, NULL);
    int out_sentinel = (out_ret == 0) ? 1 : 0;
    assert(out_ret == 0); /* EVA-proven oracle */
    assert(out_sentinel == 1); /* EVA-proven oracle */
    printf("PASS test_stack_register_protocol_valid_tv001\n");
}

static void test_stack_unregister_protocol_null_tv001(void) {
    int out_ret = ip_stack_unregister_protocol(NULL, 0);
    int out_sentinel = (out_ret == -1) ? 1 : 0;
    assert(out_ret == -1); /* EVA-proven oracle */
    assert(out_sentinel == 1); /* EVA-proven oracle */
    printf("PASS test_stack_unregister_protocol_null_tv001\n");
}

static void test_stack_unregister_protocol_valid_tv001(void) {
    IpStack stack;
    memset(&stack, 0, sizeof(stack));
    int out_ret = ip_stack_unregister_protocol(&stack, 0);
    int out_sentinel = (out_ret == 0) ? 1 : 0;
    assert(out_ret == 0); /* EVA-proven oracle */
    assert(out_sentinel == 1); /* EVA-proven oracle */
    printf("PASS test_stack_unregister_protocol_valid_tv001\n");
}

static void test_receive_null_tv001(void) {
    int out_ret = ip_receive(NULL, NULL, 0, NULL);
    int out_sentinel = (out_ret == -1) ? 1 : 0;
    assert(out_ret == -1); /* EVA-proven oracle */
    assert(out_sentinel == 1); /* EVA-proven oracle */
    printf("PASS test_receive_null_tv001\n");
}

static void test_receive_too_short_tv001(void) {
    uint8_t iface_mac[6] = {0, 0, 0, 0, 0, 0};
    Interface *iface = interface_create("kleva", iface_mac, 0x0100A8C0, 24, 1500);
    assert(iface != NULL);
    Packet *frame = packet_create(64);
    assert(frame != NULL);
    if (frame->len == 0) frame->len = 8;
    memset(frame->data, 0, frame->len);
    frame->len = ((IP_HDR_LEN) > 0 ? (IP_HDR_LEN) - 1 : 0);
    int out_ret = ip_receive(iface, frame, 0, NULL);
    int out_sentinel = (out_ret == -1) ? 1 : 0;
    assert(out_ret == -1); /* EVA-proven oracle */
    assert(out_sentinel == 1); /* EVA-proven oracle */
    interface_free(iface);
    printf("PASS test_receive_too_short_tv001\n");
}

static void test_receive_bad_version_tv001(void) {
    uint8_t iface_mac[6] = {0, 0, 0, 0, 0, 0};
    Interface *iface = interface_create("kleva", iface_mac, 0x0100A8C0, 24, 1500);
    assert(iface != NULL);
    Packet *frame = packet_create(64);
    assert(frame != NULL);
    if (frame->len == 0) frame->len = 8;
    memset(frame->data, 0, frame->len);
    frame->len = IP_HDR_LEN;
    int out_ret = ip_receive(iface, frame, 0, NULL);
    int out_sentinel = (out_ret == -1) ? 1 : 0;
    assert(out_ret == -1); /* EVA-proven oracle */
    assert(out_sentinel == 1); /* EVA-proven oracle */
    interface_free(iface);
    printf("PASS test_receive_bad_version_tv001\n");
}

static void test_receive_ttl_zero_tv001(void) {
    uint8_t iface_mac[6] = {0, 0, 0, 0, 0, 0};
    Interface *iface = interface_create("kleva", iface_mac, 0x0100A8C0, 24, 1500);
    assert(iface != NULL);
    Packet *frame = packet_create(64);
    assert(frame != NULL);
    if (frame->len == 0) frame->len = 8;
    memset(frame->data, 0, frame->len);
    frame->len = IP_HDR_LEN;
    ((IpHeader *)frame->data)->ttl = 0;
    int out_ret = ip_receive(iface, frame, 0, NULL);
    int out_sentinel = (out_ret == -1) ? 1 : 0;
    assert(out_ret == -1); /* EVA-proven oracle */
    assert(out_sentinel == 1); /* EVA-proven oracle */
    interface_free(iface);
    printf("PASS test_receive_ttl_zero_tv001\n");
}

static void test_receive_bad_checksum_tv001(void) {
    uint8_t iface_mac[6] = {0, 0, 0, 0, 0, 0};
    Interface *iface = interface_create("kleva", iface_mac, 0x0100A8C0, 24, 1500);
    assert(iface != NULL);
    Packet *frame = packet_create(64);
    assert(frame != NULL);
    if (frame->len == 0) frame->len = 8;
    memset(frame->data, 0, frame->len);
    frame->len = IP_HDR_LEN;
    ((IpHeader *)frame->data)->header_checksum = 0xFFFF;
    int out_ret = ip_receive(iface, frame, 0, NULL);
    int out_sentinel = (out_ret == -1) ? 1 : 0;
    assert(out_ret == -1); /* EVA-proven oracle */
    assert(out_sentinel == 1); /* EVA-proven oracle */
    interface_free(iface);
    printf("PASS test_receive_bad_checksum_tv001\n");
}

static void test_receive_valid_tv001(void) {
    uint8_t iface_mac[6] = {0, 0, 0, 0, 0, 0};
    Interface *iface = interface_create("kleva", iface_mac, 0x0100A8C0, 24, 1500);
    assert(iface != NULL);
    Packet *frame = packet_create(64);
    assert(frame != NULL);
    if (frame->len == 0) frame->len = 8;
    memset(frame->data, 0, frame->len);
    frame->len = 20;
    int out_ret = ip_receive(iface, frame, 0, NULL);
    int out_sentinel = (out_ret == 0) ? 1 : 0;
    assert(out_ret == -1); /* EVA-proven oracle */
    assert(out_sentinel == 0); /* EVA-proven oracle */
    interface_free(iface);
    printf("PASS test_receive_valid_tv001\n");
}

static void test_receive_source_ip_hdr_ttl_0_tv001(void) {
    uint8_t iface_mac[6] = {0, 0, 0, 0, 0, 0};
    Interface *iface = interface_create("kleva", iface_mac, 0x0100A8C0, 24, 1500);
    assert(iface != NULL);
    Packet *frame = packet_create(64);
    assert(frame != NULL);
    if (frame->len == 0) frame->len = 8;
    memset(frame->data, 0, frame->len);
    frame->len = IP_HDR_LEN;
    ((IpHeader *)frame->data)->header_checksum = 0xFFFF;
    ((IpHeader *)frame->data)->ttl = 0;
    int out_ret = ip_receive(iface, frame, 0, NULL);
    int out_sentinel = (out_ret == -1) ? 1 : 0;
    assert(out_ret == -1); /* EVA-proven oracle */
    assert(out_sentinel == 1); /* EVA-proven oracle */
    interface_free(iface);
    printf("PASS test_receive_source_ip_hdr_ttl_0_tv001\n");
}

static void test_send_null_tv001(void) {
    int out_ret = ip_send(NULL, NULL, NULL, 0, 0, 0, NULL);
    int out_sentinel = (out_ret == -1) ? 1 : 0;
    assert(out_ret == -1); /* EVA-proven oracle */
    assert(out_sentinel == 1); /* EVA-proven oracle */
    printf("PASS test_send_null_tv001\n");
}

static void test_send_valid_tv001(void) {
    Topology *sim_topo = topology_create();
    assert(sim_topo != NULL);
    Scheduler *sim_sched = scheduler_create(64);
    assert(sim_sched != NULL);
    Simulator *sim = simulator_create(sim_topo, sim_sched);
    assert(sim != NULL);
    uint8_t iface_mac[6] = {0, 0, 0, 0, 0, 0};
    Interface *iface = interface_create("kleva", iface_mac, 0x0100A8C0, 24, 1500);
    assert(iface != NULL);
    uint8_t dst_mac[6] = {0, 0, 0, 0, 0, 0};
    Packet *payload = packet_create(64);
    assert(payload != NULL);
    if (payload->len == 0) payload->len = 8;
    memset(payload->data, 0, payload->len);
    int out_ret = ip_send(sim, iface, dst_mac, 0, 0, 0, payload);
    assert(out_ret == -1); /* EVA-proven oracle */
    simulator_free(sim);
    interface_free(iface);
    packet_free(payload);
    printf("PASS test_send_valid_tv001\n");
}

static void test_send_prepend_failed_tv001(void) {
    Topology *sim_topo = topology_create();
    assert(sim_topo != NULL);
    Scheduler *sim_sched = scheduler_create(64);
    assert(sim_sched != NULL);
    Simulator *sim = simulator_create(sim_topo, sim_sched);
    assert(sim != NULL);
    uint8_t iface_mac[6] = {0, 0, 0, 0, 0, 0};
    Interface *iface = interface_create("kleva", iface_mac, 0x0100A8C0, 24, 1500);
    assert(iface != NULL);
    uint8_t dst_mac[6] = {0, 0, 0, 0, 0, 0};
    Packet *payload = packet_create(64);
    assert(payload != NULL);
    if (payload->len == 0) payload->len = 8;
    memset(payload->data, 0, payload->len);
    int out_ret = ip_send(sim, iface, dst_mac, 0, 0, 0, payload);
    int out_sentinel = (out_ret == -1) ? 1 : 0;
    assert(out_ret == -1); /* EVA-proven oracle */
    assert(out_sentinel == 1); /* EVA-proven oracle */
    simulator_free(sim);
    interface_free(iface);
    packet_free(payload);
    printf("PASS test_send_prepend_failed_tv001\n");
}

static void test_output_null_tv001(void) {
    int out_ret = ip_output(NULL, 0, 0, 0, NULL);
    int out_sentinel = (out_ret == -1) ? 1 : 0;
    assert(out_ret == -1); /* EVA-proven oracle */
    assert(out_sentinel == 1); /* EVA-proven oracle */
    printf("PASS test_output_null_tv001\n");
}

static void test_output_no_topology_tv001(void) {
    Topology *sim_topo = topology_create();
    assert(sim_topo != NULL);
    Scheduler *sim_sched = scheduler_create(64);
    assert(sim_sched != NULL);
    Simulator *sim = simulator_create(sim_topo, sim_sched);
    assert(sim != NULL);
    Packet *payload = packet_create(64);
    assert(payload != NULL);
    if (payload->len == 0) payload->len = 8;
    memset(payload->data, 0, payload->len);
    int out_ret = ip_output(sim, 0, 0, 0, payload);
    assert(out_ret == -1); /* EVA-proven oracle */
    simulator_free(sim);
    printf("PASS test_output_no_topology_tv001\n");
}

static void test_output_direct_output_tv001(void) {
    Topology *sim_topo = topology_create();
    assert(sim_topo != NULL);
    Scheduler *sim_sched = scheduler_create(64);
    assert(sim_sched != NULL);
    Simulator *sim = simulator_create(sim_topo, sim_sched);
    assert(sim != NULL);
    Packet *payload = packet_create(64);
    assert(payload != NULL);
    if (payload->len == 0) payload->len = 8;
    memset(payload->data, 0, payload->len);
    int out_ret = ip_output(sim, 0, 0, 0, payload);
    assert(out_ret == -1); /* EVA-proven oracle */
    simulator_free(sim);
    printf("PASS test_output_direct_output_tv001\n");
}

static void test_checksum_null_tv001(void) {
    uint16_t out_ret = ip_checksum(NULL);
    int out_sentinel = (out_ret == 65535) ? 1 : 0;
    assert(out_ret == 65535); /* EVA-proven oracle */
    assert(out_sentinel == 1); /* EVA-proven oracle */
    printf("PASS test_checksum_null_tv001\n");
}

static void test_checksum_valid_tv001(void) {
    IpHeader ip_hdr;
    memset(&ip_hdr, 0, sizeof(ip_hdr));
    uint16_t out_ret = ip_checksum(&ip_hdr);
    assert(out_ret == 65535); /* EVA-proven oracle */
    printf("PASS test_checksum_valid_tv001\n");
}

static void test_is_local_null_tv001(void) {
    int out_ret = ip_is_local(NULL, 0);
    int out_sentinel = (out_ret == 0) ? 1 : 0;
    assert(out_ret == 0); /* EVA-proven oracle */
    assert(out_sentinel == 1); /* EVA-proven oracle */
    printf("PASS test_is_local_null_tv001\n");
}

static void test_is_local_hit_tv001(void) {
    Device *dev = device_create("kleva", 4);
    assert(dev != NULL);
    int out_ret = ip_is_local(dev, 0);
    int out_sentinel = (out_ret == 1) ? 1 : 0;
    assert(out_ret == 0); /* EVA-proven oracle */
    assert(out_sentinel == 0); /* EVA-proven oracle */
    device_free(dev);
    printf("PASS test_is_local_hit_tv001\n");
}

static void test_is_local_miss_tv001(void) {
    Device *dev = device_create("kleva", 4);
    assert(dev != NULL);
    int out_ret = ip_is_local(dev, 0);
    int out_sentinel = (out_ret == 0) ? 1 : 0;
    assert(out_ret == 0); /* EVA-proven oracle */
    assert(out_sentinel == 1); /* EVA-proven oracle */
    device_free(dev);
    printf("PASS test_is_local_miss_tv001\n");
}

int main(void) {
    test_init_null_tv001();
    test_init_registers_handlers_tv001();
    test_stack_init_null_tv001();
    test_stack_init_valid_tv001();
    test_stack_bind_interface_null_tv001();
    test_stack_bind_interface_valid_tv001();
    test_stack_register_protocol_null_tv001();
    test_stack_register_protocol_valid_tv001();
    test_stack_unregister_protocol_null_tv001();
    test_stack_unregister_protocol_valid_tv001();
    test_receive_null_tv001();
    test_receive_too_short_tv001();
    test_receive_bad_version_tv001();
    test_receive_ttl_zero_tv001();
    test_receive_bad_checksum_tv001();
    test_receive_valid_tv001();
    test_receive_source_ip_hdr_ttl_0_tv001();
    test_send_null_tv001();
    test_send_valid_tv001();
    test_send_prepend_failed_tv001();
    test_output_null_tv001();
    test_output_no_topology_tv001();
    test_output_direct_output_tv001();
    test_checksum_null_tv001();
    test_checksum_valid_tv001();
    test_is_local_null_tv001();
    test_is_local_hit_tv001();
    test_is_local_miss_tv001();

    printf("\nAll unit tests passed.\n");
    return 0;
}
