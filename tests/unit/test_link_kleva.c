/* AUTO-GENERATED by kleva (KLEE + EVA) — DO NOT EDIT
 * Oracle values are EVA-proven singletons — not hand-written expected values.
 * Re-generate: kleva gen <module.yaml>
 * Generated: 2026-07-02 16:43:05
 */
#include <assert.h>
#include <stdio.h>
#include <string.h>
#include <stdint.h>
#include "link.h"

/* KLEVA native compile note:
 * This generated test includes allocator-failure candidates.
 * Compile the module-under-test with:
 *   -Dmalloc=__kleva_malloc -Dcalloc=__kleva_calloc -Drealloc=__kleva_realloc -Dfree=__kleva_free
 */

#include "link.h"
#include "interface.h"
#include <stddef.h>
#include <stdint.h>
#include <string.h>
#ifdef malloc
#undef malloc
#endif
#ifdef calloc
#undef calloc
#endif
#ifdef realloc
#undef realloc
#endif
#ifdef free
#undef free
#endif
static unsigned char __kleva_alloc_arena[1024u * 1024u];
struct __kleva_alloc_rec {
    void *ptr;
    size_t size;
    int active;
};
static struct __kleva_alloc_rec __kleva_alloc_recs[1024];
static size_t __kleva_alloc_offset;
static long __kleva_alloc_call;
static long __kleva_alloc_fail_at = -1;
static void __kleva_alloc_reset(void) {
    __kleva_alloc_offset = 0;
    __kleva_alloc_call = 0;
    __kleva_alloc_fail_at = -1;
    memset(__kleva_alloc_recs, 0, sizeof(__kleva_alloc_recs));
}
static void __kleva_alloc_record(void *ptr, size_t size) {
    if (!ptr) {
        return;
    }
    for (size_t i = 0; i < sizeof(__kleva_alloc_recs) / sizeof(__kleva_alloc_recs[0]); i++) {
        if (!__kleva_alloc_recs[i].active) {
            __kleva_alloc_recs[i].ptr = ptr;
            __kleva_alloc_recs[i].size = size;
            __kleva_alloc_recs[i].active = 1;
            return;
        }
    }
}
static size_t __kleva_alloc_size_of(void *ptr) {
    for (size_t i = 0; i < sizeof(__kleva_alloc_recs) / sizeof(__kleva_alloc_recs[0]); i++) {
        if (__kleva_alloc_recs[i].active && __kleva_alloc_recs[i].ptr == ptr) {
            return __kleva_alloc_recs[i].size;
        }
    }
    return 0;
}
static void __kleva_alloc_forget(void *ptr) {
    for (size_t i = 0; i < sizeof(__kleva_alloc_recs) / sizeof(__kleva_alloc_recs[0]); i++) {
        if (__kleva_alloc_recs[i].active && __kleva_alloc_recs[i].ptr == ptr) {
            __kleva_alloc_recs[i].active = 0;
            return;
        }
    }
}
static void __kleva_alloc_fail_on(long index) {
    __kleva_alloc_fail_at = __kleva_alloc_call + index;
}
void *__kleva_malloc(size_t size) {
    if (__kleva_alloc_fail_at >= 0 && __kleva_alloc_call == __kleva_alloc_fail_at) {
        __kleva_alloc_call++;
        __kleva_alloc_fail_at = -1;
        return (void *)0;
    }
    __kleva_alloc_call++;
    if (size == 0) {
        size = 1;
    }
    size = (size + 7u) & ~((size_t)7u);
    if (size > sizeof(__kleva_alloc_arena) - __kleva_alloc_offset) {
        return (void *)0;
    }
    void *ptr = __kleva_alloc_arena + __kleva_alloc_offset;
    __kleva_alloc_offset += size;
    __kleva_alloc_record(ptr, size);
    return ptr;
}
void __kleva_free(void *ptr) {
    __kleva_alloc_forget(ptr);
}
void *__kleva_calloc(size_t count, size_t size) {
    size_t total = count * size;
    void *ptr = __kleva_malloc(total);
    if (ptr) {
        memset(ptr, 0, total);
    }
    return ptr;
}
void *__kleva_realloc(void *ptr, size_t size) {
    if (!ptr) {
        return __kleva_malloc(size);
    }
    if (size == 0) {
        __kleva_free(ptr);
        return (void *)0;
    }
    size_t old_size = __kleva_alloc_size_of(ptr);
    void *new_ptr = __kleva_malloc(size);
    if (!new_ptr) {
        return (void *)0;
    }
    if (old_size > 0) {
        size_t copy_size = old_size < size ? old_size : size;
        memcpy(new_ptr, ptr, copy_size);
    }
    __kleva_alloc_forget(ptr);
    return new_ptr;
}
#include "scheduler.h"
#include "packet.h"
static void kleva_stub_EventCallback(const Event * arg0, void * arg1) {
    (void)arg0;
    (void)arg1;
}

static void test_create_null_or_bad_tv001(void) {
    Link * out_ret = link_create(NULL, NULL, 0, 0, 0);
    assert(out_ret == 0); /* EVA-proven oracle */
    printf("PASS test_create_null_or_bad_tv001\n");
}

static void test_create_ok_tv001(void) {
    Interface * interface_create(const char * name, const uint8_t * mac, uint32_t ip_addr, uint8_t prefix_len, uint16_t mtu);
    uint8_t end_a_mac_buf[64];
    memset(end_a_mac_buf, 0, sizeof(end_a_mac_buf));
    const uint8_t * end_a_mac = end_a_mac_buf;
    Interface *end_a = interface_create("kleva", end_a_mac, 1, 24, 1);
    assert(end_a != NULL);
    void interface_free(Interface *arg0);
    Interface * interface_create(const char * name, const uint8_t * mac, uint32_t ip_addr, uint8_t prefix_len, uint16_t mtu);
    uint8_t end_b_mac_buf[64];
    memset(end_b_mac_buf, 0, sizeof(end_b_mac_buf));
    const uint8_t * end_b_mac = end_b_mac_buf;
    Interface *end_b = interface_create("kleva", end_b_mac, 1, 24, 1);
    assert(end_b != NULL);
    void interface_free(Interface *arg0);
    Link * out_ret = link_create(end_a, end_b, 1, 0, 0);
    int out_ret_nonnull = (out_ret != NULL);
    int out_end_a_same = (out_ret != NULL && out_ret->end_a == end_a);
    int out_end_b_same = (out_ret != NULL && out_ret->end_b == end_b);
    assert(out_ret_nonnull == 1); /* EVA-proven oracle */
    assert(out_end_a_same == 1); /* EVA-proven oracle */
    assert(out_end_b_same == 1); /* EVA-proven oracle */
    printf("PASS test_create_ok_tv001\n");
}

static void test_create_ir_if_0_left_left_not_end_a_tv001(void) {
    Interface * interface_create(const char * name, const uint8_t * mac, uint32_t ip_addr, uint8_t prefix_len, uint16_t mtu);
    uint8_t end_a_mac_buf[64];
    memset(end_a_mac_buf, 0, sizeof(end_a_mac_buf));
    const uint8_t * end_a_mac = end_a_mac_buf;
    Interface *end_a = interface_create("kleva", end_a_mac, 1, 24, 1);
    assert(end_a != NULL);
    void interface_free(Interface *arg0);
    Interface * interface_create(const char * name, const uint8_t * mac, uint32_t ip_addr, uint8_t prefix_len, uint16_t mtu);
    uint8_t end_b_mac_buf[64];
    memset(end_b_mac_buf, 0, sizeof(end_b_mac_buf));
    const uint8_t * end_b_mac = end_b_mac_buf;
    Interface *end_b = interface_create("kleva", end_b_mac, 1, 24, 1);
    assert(end_b != NULL);
    void interface_free(Interface *arg0);
    end_a = 0;
    Link * out_ret = link_create(end_a, end_b, 1, 0, 0);
    int out_ret_nonnull = (out_ret != NULL);
    int out_end_a_same = (out_ret != NULL && out_ret->end_a == end_a);
    int out_end_b_same = (out_ret != NULL && out_ret->end_b == end_b);
    assert(out_ret_nonnull == 0); /* EVA-proven oracle */
    assert(out_end_a_same == 0); /* EVA-proven oracle */
    assert(out_end_b_same == 0); /* EVA-proven oracle */
    printf("PASS test_create_ir_if_0_left_left_not_end_a_tv001\n");
}

static void test_create_ir_if_0_left_right_not_end_b_tv001(void) {
    Interface * interface_create(const char * name, const uint8_t * mac, uint32_t ip_addr, uint8_t prefix_len, uint16_t mtu);
    uint8_t end_a_mac_buf[64];
    memset(end_a_mac_buf, 0, sizeof(end_a_mac_buf));
    const uint8_t * end_a_mac = end_a_mac_buf;
    Interface *end_a = interface_create("kleva", end_a_mac, 1, 24, 1);
    assert(end_a != NULL);
    void interface_free(Interface *arg0);
    Interface * interface_create(const char * name, const uint8_t * mac, uint32_t ip_addr, uint8_t prefix_len, uint16_t mtu);
    uint8_t end_b_mac_buf[64];
    memset(end_b_mac_buf, 0, sizeof(end_b_mac_buf));
    const uint8_t * end_b_mac = end_b_mac_buf;
    Interface *end_b = interface_create("kleva", end_b_mac, 1, 24, 1);
    assert(end_b != NULL);
    void interface_free(Interface *arg0);
    end_b = 0;
    Link * out_ret = link_create(end_a, end_b, 1, 0, 0);
    int out_ret_nonnull = (out_ret != NULL);
    int out_end_a_same = (out_ret != NULL && out_ret->end_a == end_a);
    int out_end_b_same = (out_ret != NULL && out_ret->end_b == end_b);
    assert(out_ret_nonnull == 0); /* EVA-proven oracle */
    assert(out_end_a_same == 0); /* EVA-proven oracle */
    assert(out_end_b_same == 0); /* EVA-proven oracle */
    printf("PASS test_create_ir_if_0_left_right_not_end_b_tv001\n");
}

static void test_create_ir_if_0_right_bw_eq_0_tv001(void) {
    Interface * interface_create(const char * name, const uint8_t * mac, uint32_t ip_addr, uint8_t prefix_len, uint16_t mtu);
    uint8_t end_a_mac_buf[64];
    memset(end_a_mac_buf, 0, sizeof(end_a_mac_buf));
    const uint8_t * end_a_mac = end_a_mac_buf;
    Interface *end_a = interface_create("kleva", end_a_mac, 1, 24, 1);
    assert(end_a != NULL);
    void interface_free(Interface *arg0);
    Interface * interface_create(const char * name, const uint8_t * mac, uint32_t ip_addr, uint8_t prefix_len, uint16_t mtu);
    uint8_t end_b_mac_buf[64];
    memset(end_b_mac_buf, 0, sizeof(end_b_mac_buf));
    const uint8_t * end_b_mac = end_b_mac_buf;
    Interface *end_b = interface_create("kleva", end_b_mac, 1, 24, 1);
    assert(end_b != NULL);
    void interface_free(Interface *arg0);
    Link * out_ret = link_create(end_a, end_b, 1, 0, 0);
    int out_ret_nonnull = (out_ret != NULL);
    int out_end_a_same = (out_ret != NULL && out_ret->end_a == end_a);
    int out_end_b_same = (out_ret != NULL && out_ret->end_b == end_b);
    assert(out_ret_nonnull == 1); /* EVA-proven oracle */
    assert(out_end_a_same == 1); /* EVA-proven oracle */
    assert(out_end_b_same == 1); /* EVA-proven oracle */
    printf("PASS test_create_ir_if_0_right_bw_eq_0_tv001\n");
}

static void test_create_ir_if_0_false_false_not_end_a_and_false_not_end_b_and_false_bw_ne_0_tv001(void) {
    Interface * interface_create(const char * name, const uint8_t * mac, uint32_t ip_addr, uint8_t prefix_len, uint16_t mtu);
    uint8_t end_a_mac_buf[64];
    memset(end_a_mac_buf, 0, sizeof(end_a_mac_buf));
    const uint8_t * end_a_mac = end_a_mac_buf;
    Interface *end_a = interface_create("kleva", end_a_mac, 1, 24, 1);
    assert(end_a != NULL);
    void interface_free(Interface *arg0);
    Interface * interface_create(const char * name, const uint8_t * mac, uint32_t ip_addr, uint8_t prefix_len, uint16_t mtu);
    uint8_t end_b_mac_buf[64];
    memset(end_b_mac_buf, 0, sizeof(end_b_mac_buf));
    const uint8_t * end_b_mac = end_b_mac_buf;
    Interface *end_b = interface_create("kleva", end_b_mac, 1, 24, 1);
    assert(end_b != NULL);
    void interface_free(Interface *arg0);
    assert(end_a != NULL);
    assert(end_b != NULL);
    Link * out_ret = link_create(end_a, end_b, 1, 0, 0);
    int out_ret_nonnull = (out_ret != NULL);
    int out_end_a_same = (out_ret != NULL && out_ret->end_a == end_a);
    int out_end_b_same = (out_ret != NULL && out_ret->end_b == end_b);
    assert(out_ret_nonnull == 1); /* EVA-proven oracle */
    assert(out_end_a_same == 1); /* EVA-proven oracle */
    assert(out_end_b_same == 1); /* EVA-proven oracle */
    printf("PASS test_create_ir_if_0_false_false_not_end_a_and_false_not_end_b_and_false_bw_ne_0_tv001\n");
}

static void test_create_ir_alloc_malloc_0_zero_failure_tv001(void) {
    __kleva_alloc_reset();
    Interface * interface_create(const char * name, const uint8_t * mac, uint32_t ip_addr, uint8_t prefix_len, uint16_t mtu);
    uint8_t end_a_mac_buf[64];
    memset(end_a_mac_buf, 0, sizeof(end_a_mac_buf));
    const uint8_t * end_a_mac = end_a_mac_buf;
    Interface *end_a = interface_create("kleva", end_a_mac, 1, 24, 1);
    assert(end_a != NULL);
    void interface_free(Interface *arg0);
    Interface * interface_create(const char * name, const uint8_t * mac, uint32_t ip_addr, uint8_t prefix_len, uint16_t mtu);
    uint8_t end_b_mac_buf[64];
    memset(end_b_mac_buf, 0, sizeof(end_b_mac_buf));
    const uint8_t * end_b_mac = end_b_mac_buf;
    Interface *end_b = interface_create("kleva", end_b_mac, 1, 24, 1);
    assert(end_b != NULL);
    void interface_free(Interface *arg0);
    __kleva_alloc_fail_on(0);
    Link * out_ret = link_create(end_a, end_b, 1, 0, 0);
    int out_ret_nonnull = (out_ret != NULL);
    int out_end_a_same = (out_ret != NULL && out_ret->end_a == end_a);
    int out_end_b_same = (out_ret != NULL && out_ret->end_b == end_b);
    assert(out_ret_nonnull == 0); /* EVA-proven oracle */
    assert(out_end_a_same == 0); /* EVA-proven oracle */
    assert(out_end_b_same == 0); /* EVA-proven oracle */
    printf("PASS test_create_ir_alloc_malloc_0_zero_failure_tv001\n");
}

static void test_create_ir_forbidden_value_0_bw_eq_0_forbidden_tv001(void) {
    Interface * interface_create(const char * name, const uint8_t * mac, uint32_t ip_addr, uint8_t prefix_len, uint16_t mtu);
    uint8_t end_a_mac_buf[64];
    memset(end_a_mac_buf, 0, sizeof(end_a_mac_buf));
    const uint8_t * end_a_mac = end_a_mac_buf;
    Interface *end_a = interface_create("kleva", end_a_mac, 1, 24, 1);
    assert(end_a != NULL);
    void interface_free(Interface *arg0);
    Interface * interface_create(const char * name, const uint8_t * mac, uint32_t ip_addr, uint8_t prefix_len, uint16_t mtu);
    uint8_t end_b_mac_buf[64];
    memset(end_b_mac_buf, 0, sizeof(end_b_mac_buf));
    const uint8_t * end_b_mac = end_b_mac_buf;
    Interface *end_b = interface_create("kleva", end_b_mac, 1, 24, 1);
    assert(end_b != NULL);
    void interface_free(Interface *arg0);
    Link * out_ret = link_create(end_a, end_b, 1, 0, 0);
    int out_ret_nonnull = (out_ret != NULL);
    int out_end_a_same = (out_ret != NULL && out_ret->end_a == end_a);
    int out_end_b_same = (out_ret != NULL && out_ret->end_b == end_b);
    assert(out_ret_nonnull == 1); /* EVA-proven oracle */
    assert(out_end_a_same == 1); /* EVA-proven oracle */
    assert(out_end_b_same == 1); /* EVA-proven oracle */
    printf("PASS test_create_ir_forbidden_value_0_bw_eq_0_forbidden_tv001\n");
}

static void test_create_ir_forbidden_value_0_bw_eq_0_allowed_tv001(void) {
    Interface * interface_create(const char * name, const uint8_t * mac, uint32_t ip_addr, uint8_t prefix_len, uint16_t mtu);
    uint8_t end_a_mac_buf[64];
    memset(end_a_mac_buf, 0, sizeof(end_a_mac_buf));
    const uint8_t * end_a_mac = end_a_mac_buf;
    Interface *end_a = interface_create("kleva", end_a_mac, 1, 24, 1);
    assert(end_a != NULL);
    void interface_free(Interface *arg0);
    Interface * interface_create(const char * name, const uint8_t * mac, uint32_t ip_addr, uint8_t prefix_len, uint16_t mtu);
    uint8_t end_b_mac_buf[64];
    memset(end_b_mac_buf, 0, sizeof(end_b_mac_buf));
    const uint8_t * end_b_mac = end_b_mac_buf;
    Interface *end_b = interface_create("kleva", end_b_mac, 1, 24, 1);
    assert(end_b != NULL);
    void interface_free(Interface *arg0);
    Link * out_ret = link_create(end_a, end_b, 1, 0, 0);
    int out_ret_nonnull = (out_ret != NULL);
    int out_end_a_same = (out_ret != NULL && out_ret->end_a == end_a);
    int out_end_b_same = (out_ret != NULL && out_ret->end_b == end_b);
    assert(out_ret_nonnull == 1); /* EVA-proven oracle */
    assert(out_end_a_same == 1); /* EVA-proven oracle */
    assert(out_end_b_same == 1); /* EVA-proven oracle */
    printf("PASS test_create_ir_forbidden_value_0_bw_eq_0_allowed_tv001\n");
}

static void test_create_ir_diversity_bw_one_tv001(void) {
    Interface * interface_create(const char * name, const uint8_t * mac, uint32_t ip_addr, uint8_t prefix_len, uint16_t mtu);
    uint8_t end_a_mac_buf[64];
    memset(end_a_mac_buf, 0, sizeof(end_a_mac_buf));
    const uint8_t * end_a_mac = end_a_mac_buf;
    Interface *end_a = interface_create("kleva", end_a_mac, 1, 24, 1);
    assert(end_a != NULL);
    void interface_free(Interface *arg0);
    Interface * interface_create(const char * name, const uint8_t * mac, uint32_t ip_addr, uint8_t prefix_len, uint16_t mtu);
    uint8_t end_b_mac_buf[64];
    memset(end_b_mac_buf, 0, sizeof(end_b_mac_buf));
    const uint8_t * end_b_mac = end_b_mac_buf;
    Interface *end_b = interface_create("kleva", end_b_mac, 1, 24, 1);
    assert(end_b != NULL);
    void interface_free(Interface *arg0);
    Link * out_ret = link_create(end_a, end_b, 1, 0, 0);
    int out_ret_nonnull = (out_ret != NULL);
    int out_end_a_same = (out_ret != NULL && out_ret->end_a == end_a);
    int out_end_b_same = (out_ret != NULL && out_ret->end_b == end_b);
    assert(out_ret_nonnull == 1); /* EVA-proven oracle */
    assert(out_end_a_same == 1); /* EVA-proven oracle */
    assert(out_end_b_same == 1); /* EVA-proven oracle */
    printf("PASS test_create_ir_diversity_bw_one_tv001\n");
}

static void test_create_ir_diversity_delay_min_tv001(void) {
    Interface * interface_create(const char * name, const uint8_t * mac, uint32_t ip_addr, uint8_t prefix_len, uint16_t mtu);
    uint8_t end_a_mac_buf[64];
    memset(end_a_mac_buf, 0, sizeof(end_a_mac_buf));
    const uint8_t * end_a_mac = end_a_mac_buf;
    Interface *end_a = interface_create("kleva", end_a_mac, 1, 24, 1);
    assert(end_a != NULL);
    void interface_free(Interface *arg0);
    Interface * interface_create(const char * name, const uint8_t * mac, uint32_t ip_addr, uint8_t prefix_len, uint16_t mtu);
    uint8_t end_b_mac_buf[64];
    memset(end_b_mac_buf, 0, sizeof(end_b_mac_buf));
    const uint8_t * end_b_mac = end_b_mac_buf;
    Interface *end_b = interface_create("kleva", end_b_mac, 1, 24, 1);
    assert(end_b != NULL);
    void interface_free(Interface *arg0);
    Link * out_ret = link_create(end_a, end_b, 1, 0, 0);
    int out_ret_nonnull = (out_ret != NULL);
    int out_end_a_same = (out_ret != NULL && out_ret->end_a == end_a);
    int out_end_b_same = (out_ret != NULL && out_ret->end_b == end_b);
    assert(out_ret_nonnull == 1); /* EVA-proven oracle */
    assert(out_end_a_same == 1); /* EVA-proven oracle */
    assert(out_end_b_same == 1); /* EVA-proven oracle */
    printf("PASS test_create_ir_diversity_delay_min_tv001\n");
}

static void test_create_ir_diversity_delay_one_tv001(void) {
    Interface * interface_create(const char * name, const uint8_t * mac, uint32_t ip_addr, uint8_t prefix_len, uint16_t mtu);
    uint8_t end_a_mac_buf[64];
    memset(end_a_mac_buf, 0, sizeof(end_a_mac_buf));
    const uint8_t * end_a_mac = end_a_mac_buf;
    Interface *end_a = interface_create("kleva", end_a_mac, 1, 24, 1);
    assert(end_a != NULL);
    void interface_free(Interface *arg0);
    Interface * interface_create(const char * name, const uint8_t * mac, uint32_t ip_addr, uint8_t prefix_len, uint16_t mtu);
    uint8_t end_b_mac_buf[64];
    memset(end_b_mac_buf, 0, sizeof(end_b_mac_buf));
    const uint8_t * end_b_mac = end_b_mac_buf;
    Interface *end_b = interface_create("kleva", end_b_mac, 1, 24, 1);
    assert(end_b != NULL);
    void interface_free(Interface *arg0);
    Link * out_ret = link_create(end_a, end_b, 1, 1, 0);
    int out_ret_nonnull = (out_ret != NULL);
    int out_end_a_same = (out_ret != NULL && out_ret->end_a == end_a);
    int out_end_b_same = (out_ret != NULL && out_ret->end_b == end_b);
    assert(out_ret_nonnull == 1); /* EVA-proven oracle */
    assert(out_end_a_same == 1); /* EVA-proven oracle */
    assert(out_end_b_same == 1); /* EVA-proven oracle */
    printf("PASS test_create_ir_diversity_delay_one_tv001\n");
}

static void test_free_valid_tv001(void) {
    link_free(NULL);
    int out_call_completed = 1;
    assert(out_call_completed == 1); /* EVA-proven oracle */
    printf("PASS test_free_valid_tv001\n");
}

static void test_set_up_null_tv001(void) {
    link_set_up(NULL, 0);
    int out_call_completed = 1;
    assert(out_call_completed == 1); /* EVA-proven oracle */
    printf("PASS test_set_up_null_tv001\n");
}

static void test_set_up_valid_tv001(void) {
    Link * link_create(Interface * end_a, Interface * end_b, uint32_t bw, uint32_t delay, float loss_rate);
    Interface * interface_create(const char * name, const uint8_t * mac, uint32_t ip_addr, uint8_t prefix_len, uint16_t mtu);
    uint8_t link_end_a_mac_buf[64];
    memset(link_end_a_mac_buf, 0, sizeof(link_end_a_mac_buf));
    const uint8_t * link_end_a_mac = link_end_a_mac_buf;
    Interface *link_end_a = interface_create("kleva", link_end_a_mac, 1, 24, 1);
    assert(link_end_a != NULL);
    void interface_free(Interface *arg0);
    Interface * interface_create(const char * name, const uint8_t * mac, uint32_t ip_addr, uint8_t prefix_len, uint16_t mtu);
    uint8_t link_end_b_mac_buf[64];
    memset(link_end_b_mac_buf, 0, sizeof(link_end_b_mac_buf));
    const uint8_t * link_end_b_mac = link_end_b_mac_buf;
    Interface *link_end_b = interface_create("kleva", link_end_b_mac, 1, 24, 1);
    assert(link_end_b != NULL);
    void interface_free(Interface *arg0);
    Link *link = link_create(link_end_a, link_end_b, 1, 1, 0);
    assert(link != NULL);
    void link_free(Link *arg0);
    link_set_up(link, 0);
    int out_link_end_a_nonnull = (link->end_a != NULL);
    int out_link_end_b_nonnull = (link->end_b != NULL);
    uint32_t out_link_bandwidth_mbps = link->bandwidth_mbps;
    uint32_t out_link_delay_ms = link->delay_ms;
    int out_link_loss_rate = link->loss_rate;
    int out_link_up = link->up;
    /* oracle-deferred: post-call witness will be emitted below */
    int out_ir_post_link_up = ((uintptr_t)(link->up) == (uintptr_t)(0));
    assert(out_link_end_a_nonnull == 1); /* EVA-proven oracle */
    assert(out_link_end_b_nonnull == 1); /* EVA-proven oracle */
    assert(out_link_bandwidth_mbps == 1); /* EVA-proven oracle */
    assert(out_link_delay_ms == 1); /* EVA-proven oracle */
    assert(out_link_loss_rate == 0); /* EVA-proven oracle */
    assert(out_link_up == 0); /* EVA-proven oracle */
    assert(out_ir_post_link_up == 1); /* EVA-proven oracle */
    link_free(link);
    printf("PASS test_set_up_valid_tv001\n");
}

static void test_set_up_ir_if_0_truthy_link_tv001(void) {
    Link * link_create(Interface * end_a, Interface * end_b, uint32_t bw, uint32_t delay, float loss_rate);
    Interface * interface_create(const char * name, const uint8_t * mac, uint32_t ip_addr, uint8_t prefix_len, uint16_t mtu);
    uint8_t link_end_a_mac_buf[64];
    memset(link_end_a_mac_buf, 0, sizeof(link_end_a_mac_buf));
    const uint8_t * link_end_a_mac = link_end_a_mac_buf;
    Interface *link_end_a = interface_create("kleva", link_end_a_mac, 1, 24, 1);
    assert(link_end_a != NULL);
    void interface_free(Interface *arg0);
    Interface * interface_create(const char * name, const uint8_t * mac, uint32_t ip_addr, uint8_t prefix_len, uint16_t mtu);
    uint8_t link_end_b_mac_buf[64];
    memset(link_end_b_mac_buf, 0, sizeof(link_end_b_mac_buf));
    const uint8_t * link_end_b_mac = link_end_b_mac_buf;
    Interface *link_end_b = interface_create("kleva", link_end_b_mac, 1, 24, 1);
    assert(link_end_b != NULL);
    void interface_free(Interface *arg0);
    Link *link = link_create(link_end_a, link_end_b, 1, 1, 0);
    assert(link != NULL);
    void link_free(Link *arg0);
    assert(link != NULL);
    link_set_up(link, 0);
    /* oracle-deferred: post-call witness will be emitted below */
    int out_post_state_link_up_up = ((uintptr_t)(link->up) == (uintptr_t)(0));
    assert(out_post_state_link_up_up == 1); /* EVA-proven oracle */
    printf("PASS test_set_up_ir_if_0_truthy_link_tv001\n");
}

static void test_set_up_ir_if_0_false_truthy_link_tv001(void) {
    Link * link_create(Interface * end_a, Interface * end_b, uint32_t bw, uint32_t delay, float loss_rate);
    Interface * interface_create(const char * name, const uint8_t * mac, uint32_t ip_addr, uint8_t prefix_len, uint16_t mtu);
    uint8_t link_end_a_mac_buf[64];
    memset(link_end_a_mac_buf, 0, sizeof(link_end_a_mac_buf));
    const uint8_t * link_end_a_mac = link_end_a_mac_buf;
    Interface *link_end_a = interface_create("kleva", link_end_a_mac, 1, 24, 1);
    assert(link_end_a != NULL);
    void interface_free(Interface *arg0);
    Interface * interface_create(const char * name, const uint8_t * mac, uint32_t ip_addr, uint8_t prefix_len, uint16_t mtu);
    uint8_t link_end_b_mac_buf[64];
    memset(link_end_b_mac_buf, 0, sizeof(link_end_b_mac_buf));
    const uint8_t * link_end_b_mac = link_end_b_mac_buf;
    Interface *link_end_b = interface_create("kleva", link_end_b_mac, 1, 24, 1);
    assert(link_end_b != NULL);
    void interface_free(Interface *arg0);
    Link *link = link_create(link_end_a, link_end_b, 1, 1, 0);
    assert(link != NULL);
    void link_free(Link *arg0);
    link = 0;
    link_set_up(link, 0);
    int out_call_completed = 1;
    assert(out_call_completed == 1); /* EVA-proven oracle */
    printf("PASS test_set_up_ir_if_0_false_truthy_link_tv001\n");
}

static void test_set_up_ir_diversity_up_min_tv001(void) {
    Link * link_create(Interface * end_a, Interface * end_b, uint32_t bw, uint32_t delay, float loss_rate);
    Interface * interface_create(const char * name, const uint8_t * mac, uint32_t ip_addr, uint8_t prefix_len, uint16_t mtu);
    uint8_t link_end_a_mac_buf[64];
    memset(link_end_a_mac_buf, 0, sizeof(link_end_a_mac_buf));
    const uint8_t * link_end_a_mac = link_end_a_mac_buf;
    Interface *link_end_a = interface_create("kleva", link_end_a_mac, 1, 24, 1);
    assert(link_end_a != NULL);
    void interface_free(Interface *arg0);
    Interface * interface_create(const char * name, const uint8_t * mac, uint32_t ip_addr, uint8_t prefix_len, uint16_t mtu);
    uint8_t link_end_b_mac_buf[64];
    memset(link_end_b_mac_buf, 0, sizeof(link_end_b_mac_buf));
    const uint8_t * link_end_b_mac = link_end_b_mac_buf;
    Interface *link_end_b = interface_create("kleva", link_end_b_mac, 1, 24, 1);
    assert(link_end_b != NULL);
    void interface_free(Interface *arg0);
    Link *link = link_create(link_end_a, link_end_b, 1, 1, 0);
    assert(link != NULL);
    void link_free(Link *arg0);
    link_set_up(link, 0);
    int out_call_completed = 1;
    assert(out_call_completed == 1); /* EVA-proven oracle */
    link_free(link);
    printf("PASS test_set_up_ir_diversity_up_min_tv001\n");
}

static void test_set_up_ir_diversity_up_one_tv001(void) {
    Link * link_create(Interface * end_a, Interface * end_b, uint32_t bw, uint32_t delay, float loss_rate);
    Interface * interface_create(const char * name, const uint8_t * mac, uint32_t ip_addr, uint8_t prefix_len, uint16_t mtu);
    uint8_t link_end_a_mac_buf[64];
    memset(link_end_a_mac_buf, 0, sizeof(link_end_a_mac_buf));
    const uint8_t * link_end_a_mac = link_end_a_mac_buf;
    Interface *link_end_a = interface_create("kleva", link_end_a_mac, 1, 24, 1);
    assert(link_end_a != NULL);
    void interface_free(Interface *arg0);
    Interface * interface_create(const char * name, const uint8_t * mac, uint32_t ip_addr, uint8_t prefix_len, uint16_t mtu);
    uint8_t link_end_b_mac_buf[64];
    memset(link_end_b_mac_buf, 0, sizeof(link_end_b_mac_buf));
    const uint8_t * link_end_b_mac = link_end_b_mac_buf;
    Interface *link_end_b = interface_create("kleva", link_end_b_mac, 1, 24, 1);
    assert(link_end_b != NULL);
    void interface_free(Interface *arg0);
    Link *link = link_create(link_end_a, link_end_b, 1, 1, 0);
    assert(link != NULL);
    void link_free(Link *arg0);
    link_set_up(link, 1);
    int out_call_completed = 1;
    assert(out_call_completed == 1); /* EVA-proven oracle */
    link_free(link);
    printf("PASS test_set_up_ir_diversity_up_one_tv001\n");
}

static void test_is_up_null_tv001(void) {
    int out_ret = link_is_up(NULL);
    int out_sentinel = (out_ret == 0) ? 1 : 0;
    assert(out_ret == 0); /* EVA-proven oracle */
    assert(out_sentinel == 1); /* EVA-proven oracle */
    printf("PASS test_is_up_null_tv001\n");
}

static void test_is_up_valid_tv001(void) {
    Link * link_create(Interface * end_a, Interface * end_b, uint32_t bw, uint32_t delay, float loss_rate);
    Interface * interface_create(const char * name, const uint8_t * mac, uint32_t ip_addr, uint8_t prefix_len, uint16_t mtu);
    uint8_t link_end_a_mac_buf[64];
    memset(link_end_a_mac_buf, 0, sizeof(link_end_a_mac_buf));
    const uint8_t * link_end_a_mac = link_end_a_mac_buf;
    Interface *link_end_a = interface_create("kleva", link_end_a_mac, 1, 24, 1);
    assert(link_end_a != NULL);
    void interface_free(Interface *arg0);
    Interface * interface_create(const char * name, const uint8_t * mac, uint32_t ip_addr, uint8_t prefix_len, uint16_t mtu);
    uint8_t link_end_b_mac_buf[64];
    memset(link_end_b_mac_buf, 0, sizeof(link_end_b_mac_buf));
    const uint8_t * link_end_b_mac = link_end_b_mac_buf;
    Interface *link_end_b = interface_create("kleva", link_end_b_mac, 1, 24, 1);
    assert(link_end_b != NULL);
    void interface_free(Interface *arg0);
    Link *link = link_create(link_end_a, link_end_b, 1, 1, 0);
    assert(link != NULL);
    void link_free(Link *arg0);
    int out_ret = link_is_up(link);
    assert(out_ret == 1); /* EVA-proven oracle */
    link_free(link);
    printf("PASS test_is_up_valid_tv001\n");
}

static void test_is_up_ir_if_0_truthy_link_tv001(void) {
    Link * link_create(Interface * end_a, Interface * end_b, uint32_t bw, uint32_t delay, float loss_rate);
    Interface * interface_create(const char * name, const uint8_t * mac, uint32_t ip_addr, uint8_t prefix_len, uint16_t mtu);
    uint8_t link_end_a_mac_buf[64];
    memset(link_end_a_mac_buf, 0, sizeof(link_end_a_mac_buf));
    const uint8_t * link_end_a_mac = link_end_a_mac_buf;
    Interface *link_end_a = interface_create("kleva", link_end_a_mac, 1, 24, 1);
    assert(link_end_a != NULL);
    void interface_free(Interface *arg0);
    Interface * interface_create(const char * name, const uint8_t * mac, uint32_t ip_addr, uint8_t prefix_len, uint16_t mtu);
    uint8_t link_end_b_mac_buf[64];
    memset(link_end_b_mac_buf, 0, sizeof(link_end_b_mac_buf));
    const uint8_t * link_end_b_mac = link_end_b_mac_buf;
    Interface *link_end_b = interface_create("kleva", link_end_b_mac, 1, 24, 1);
    assert(link_end_b != NULL);
    void interface_free(Interface *arg0);
    Link *link = link_create(link_end_a, link_end_b, 1, 1, 0);
    assert(link != NULL);
    void link_free(Link *arg0);
    assert(link != NULL);
    int out_ret = link_is_up(link);
    assert(out_ret == 1); /* EVA-proven oracle */
    link_free(link);
    printf("PASS test_is_up_ir_if_0_truthy_link_tv001\n");
}

static void test_is_up_ir_if_0_false_truthy_link_tv001(void) {
    Link * link_create(Interface * end_a, Interface * end_b, uint32_t bw, uint32_t delay, float loss_rate);
    Interface * interface_create(const char * name, const uint8_t * mac, uint32_t ip_addr, uint8_t prefix_len, uint16_t mtu);
    uint8_t link_end_a_mac_buf[64];
    memset(link_end_a_mac_buf, 0, sizeof(link_end_a_mac_buf));
    const uint8_t * link_end_a_mac = link_end_a_mac_buf;
    Interface *link_end_a = interface_create("kleva", link_end_a_mac, 1, 24, 1);
    assert(link_end_a != NULL);
    void interface_free(Interface *arg0);
    Interface * interface_create(const char * name, const uint8_t * mac, uint32_t ip_addr, uint8_t prefix_len, uint16_t mtu);
    uint8_t link_end_b_mac_buf[64];
    memset(link_end_b_mac_buf, 0, sizeof(link_end_b_mac_buf));
    const uint8_t * link_end_b_mac = link_end_b_mac_buf;
    Interface *link_end_b = interface_create("kleva", link_end_b_mac, 1, 24, 1);
    assert(link_end_b != NULL);
    void interface_free(Interface *arg0);
    Link *link = link_create(link_end_a, link_end_b, 1, 1, 0);
    assert(link != NULL);
    void link_free(Link *arg0);
    link = 0;
    int out_ret = link_is_up(link);
    assert(out_ret == 0); /* EVA-proven oracle */
    link_free(link);
    printf("PASS test_is_up_ir_if_0_false_truthy_link_tv001\n");
}

static void test_get_other_interface_null_tv001(void) {
    Interface * out_ret = link_get_other_interface(NULL, NULL);
    assert(out_ret == 0); /* EVA-proven oracle */
    printf("PASS test_get_other_interface_null_tv001\n");
}

static void test_get_other_interface_mismatch_tv001(void) {
    Link * link_create(Interface * end_a, Interface * end_b, uint32_t bw, uint32_t delay, float loss_rate);
    Interface * interface_create(const char * name, const uint8_t * mac, uint32_t ip_addr, uint8_t prefix_len, uint16_t mtu);
    uint8_t link_end_a_mac_buf[64];
    memset(link_end_a_mac_buf, 0, sizeof(link_end_a_mac_buf));
    const uint8_t * link_end_a_mac = link_end_a_mac_buf;
    Interface *link_end_a = interface_create("kleva", link_end_a_mac, 1, 24, 1);
    assert(link_end_a != NULL);
    void interface_free(Interface *arg0);
    Interface * interface_create(const char * name, const uint8_t * mac, uint32_t ip_addr, uint8_t prefix_len, uint16_t mtu);
    uint8_t link_end_b_mac_buf[64];
    memset(link_end_b_mac_buf, 0, sizeof(link_end_b_mac_buf));
    const uint8_t * link_end_b_mac = link_end_b_mac_buf;
    Interface *link_end_b = interface_create("kleva", link_end_b_mac, 1, 24, 1);
    assert(link_end_b != NULL);
    void interface_free(Interface *arg0);
    Link *link = link_create(link_end_a, link_end_b, 1, 1, 0);
    assert(link != NULL);
    void link_free(Link *arg0);
    Interface * interface_create(const char * name, const uint8_t * mac, uint32_t ip_addr, uint8_t prefix_len, uint16_t mtu);
    uint8_t src_mac_buf[64];
    memset(src_mac_buf, 0, sizeof(src_mac_buf));
    const uint8_t * src_mac = src_mac_buf;
    Interface *src = interface_create("kleva", src_mac, 1, 24, 1);
    assert(src != NULL);
    void interface_free(Interface *arg0);
    Interface * out_ret = link_get_other_interface(link, src);
    int out_ret_nonnull = (out_ret != NULL);
    int out_link_same = (out_ret != NULL && out_ret->link == link);
    assert(out_ret_nonnull == 0); /* EVA-proven oracle */
    assert(out_link_same == 0); /* EVA-proven oracle */
    link_free(link);
    interface_free(src);
    printf("PASS test_get_other_interface_mismatch_tv001\n");
}

static void test_get_other_interface_valid_tv001(void) {
    Link * link_create(Interface * end_a, Interface * end_b, uint32_t bw, uint32_t delay, float loss_rate);
    Interface * interface_create(const char * name, const uint8_t * mac, uint32_t ip_addr, uint8_t prefix_len, uint16_t mtu);
    uint8_t link_end_a_mac_buf[64];
    memset(link_end_a_mac_buf, 0, sizeof(link_end_a_mac_buf));
    const uint8_t * link_end_a_mac = link_end_a_mac_buf;
    Interface *link_end_a = interface_create("kleva", link_end_a_mac, 1, 24, 1);
    assert(link_end_a != NULL);
    void interface_free(Interface *arg0);
    Interface * interface_create(const char * name, const uint8_t * mac, uint32_t ip_addr, uint8_t prefix_len, uint16_t mtu);
    uint8_t link_end_b_mac_buf[64];
    memset(link_end_b_mac_buf, 0, sizeof(link_end_b_mac_buf));
    const uint8_t * link_end_b_mac = link_end_b_mac_buf;
    Interface *link_end_b = interface_create("kleva", link_end_b_mac, 1, 24, 1);
    assert(link_end_b != NULL);
    void interface_free(Interface *arg0);
    Link *link = link_create(link_end_a, link_end_b, 1, 1, 0);
    assert(link != NULL);
    void link_free(Link *arg0);
    Interface * interface_create(const char * name, const uint8_t * mac, uint32_t ip_addr, uint8_t prefix_len, uint16_t mtu);
    uint8_t src_mac_buf[64];
    memset(src_mac_buf, 0, sizeof(src_mac_buf));
    const uint8_t * src_mac = src_mac_buf;
    Interface *src = interface_create("kleva", src_mac, 1, 24, 1);
    assert(src != NULL);
    void interface_free(Interface *arg0);
    Interface * out_ret = link_get_other_interface(link, src);
    int out_ret_nonnull = (out_ret != NULL);
    int out_link_same = (out_ret != NULL && out_ret->link == link);
    assert(out_ret_nonnull == 0); /* EVA-proven oracle */
    assert(out_link_same == 0); /* EVA-proven oracle */
    link_free(link);
    interface_free(src);
    printf("PASS test_get_other_interface_valid_tv001\n");
}

static void test_get_other_interface_ir_if_0_left_not_link_tv001(void) {
    Link * link_create(Interface * end_a, Interface * end_b, uint32_t bw, uint32_t delay, float loss_rate);
    Interface * interface_create(const char * name, const uint8_t * mac, uint32_t ip_addr, uint8_t prefix_len, uint16_t mtu);
    uint8_t link_end_a_mac_buf[64];
    memset(link_end_a_mac_buf, 0, sizeof(link_end_a_mac_buf));
    const uint8_t * link_end_a_mac = link_end_a_mac_buf;
    Interface *link_end_a = interface_create("kleva", link_end_a_mac, 1, 24, 1);
    assert(link_end_a != NULL);
    void interface_free(Interface *arg0);
    Interface * interface_create(const char * name, const uint8_t * mac, uint32_t ip_addr, uint8_t prefix_len, uint16_t mtu);
    uint8_t link_end_b_mac_buf[64];
    memset(link_end_b_mac_buf, 0, sizeof(link_end_b_mac_buf));
    const uint8_t * link_end_b_mac = link_end_b_mac_buf;
    Interface *link_end_b = interface_create("kleva", link_end_b_mac, 1, 24, 1);
    assert(link_end_b != NULL);
    void interface_free(Interface *arg0);
    Link *link = link_create(link_end_a, link_end_b, 1, 1, 0);
    assert(link != NULL);
    void link_free(Link *arg0);
    Interface * interface_create(const char * name, const uint8_t * mac, uint32_t ip_addr, uint8_t prefix_len, uint16_t mtu);
    uint8_t src_mac_buf[64];
    memset(src_mac_buf, 0, sizeof(src_mac_buf));
    const uint8_t * src_mac = src_mac_buf;
    Interface *src = interface_create("kleva", src_mac, 1, 24, 1);
    assert(src != NULL);
    void interface_free(Interface *arg0);
    link = 0;
    Interface * out_ret = link_get_other_interface(link, src);
    int out_ret_nonnull = (out_ret != NULL);
    int out_link_same = (out_ret != NULL && out_ret->link == link);
    assert(out_ret_nonnull == 0); /* EVA-proven oracle */
    assert(out_link_same == 0); /* EVA-proven oracle */
    link_free(link);
    interface_free(src);
    printf("PASS test_get_other_interface_ir_if_0_left_not_link_tv001\n");
}

static void test_get_other_interface_ir_if_0_right_not_src_tv001(void) {
    Link * link_create(Interface * end_a, Interface * end_b, uint32_t bw, uint32_t delay, float loss_rate);
    Interface * interface_create(const char * name, const uint8_t * mac, uint32_t ip_addr, uint8_t prefix_len, uint16_t mtu);
    uint8_t link_end_a_mac_buf[64];
    memset(link_end_a_mac_buf, 0, sizeof(link_end_a_mac_buf));
    const uint8_t * link_end_a_mac = link_end_a_mac_buf;
    Interface *link_end_a = interface_create("kleva", link_end_a_mac, 1, 24, 1);
    assert(link_end_a != NULL);
    void interface_free(Interface *arg0);
    Interface * interface_create(const char * name, const uint8_t * mac, uint32_t ip_addr, uint8_t prefix_len, uint16_t mtu);
    uint8_t link_end_b_mac_buf[64];
    memset(link_end_b_mac_buf, 0, sizeof(link_end_b_mac_buf));
    const uint8_t * link_end_b_mac = link_end_b_mac_buf;
    Interface *link_end_b = interface_create("kleva", link_end_b_mac, 1, 24, 1);
    assert(link_end_b != NULL);
    void interface_free(Interface *arg0);
    Link *link = link_create(link_end_a, link_end_b, 1, 1, 0);
    assert(link != NULL);
    void link_free(Link *arg0);
    Interface * interface_create(const char * name, const uint8_t * mac, uint32_t ip_addr, uint8_t prefix_len, uint16_t mtu);
    uint8_t src_mac_buf[64];
    memset(src_mac_buf, 0, sizeof(src_mac_buf));
    const uint8_t * src_mac = src_mac_buf;
    Interface *src = interface_create("kleva", src_mac, 1, 24, 1);
    assert(src != NULL);
    void interface_free(Interface *arg0);
    src = 0;
    Interface * out_ret = link_get_other_interface(link, src);
    int out_ret_nonnull = (out_ret != NULL);
    int out_link_same = (out_ret != NULL && out_ret->link == link);
    assert(out_ret_nonnull == 0); /* EVA-proven oracle */
    assert(out_link_same == 0); /* EVA-proven oracle */
    link_free(link);
    interface_free(src);
    printf("PASS test_get_other_interface_ir_if_0_right_not_src_tv001\n");
}

static void test_transmit_null_tv001(void) {
    int out_ret = link_transmit(NULL, NULL, NULL, NULL, 0, NULL, NULL);
    int out_sentinel = (out_ret == -1) ? 1 : 0;
    assert(out_ret == -1); /* EVA-proven oracle */
    assert(out_sentinel == 1); /* EVA-proven oracle */
    printf("PASS test_transmit_null_tv001\n");
}

static void test_transmit_valid_tv001(void) {
    Link * link_create(Interface * end_a, Interface * end_b, uint32_t bw, uint32_t delay, float loss_rate);
    Interface * interface_create(const char * name, const uint8_t * mac, uint32_t ip_addr, uint8_t prefix_len, uint16_t mtu);
    uint8_t link_end_a_mac_buf[64];
    memset(link_end_a_mac_buf, 0, sizeof(link_end_a_mac_buf));
    const uint8_t * link_end_a_mac = link_end_a_mac_buf;
    Interface *link_end_a = interface_create("kleva", link_end_a_mac, 1, 24, 1);
    assert(link_end_a != NULL);
    void interface_free(Interface *arg0);
    Interface * interface_create(const char * name, const uint8_t * mac, uint32_t ip_addr, uint8_t prefix_len, uint16_t mtu);
    uint8_t link_end_b_mac_buf[64];
    memset(link_end_b_mac_buf, 0, sizeof(link_end_b_mac_buf));
    const uint8_t * link_end_b_mac = link_end_b_mac_buf;
    Interface *link_end_b = interface_create("kleva", link_end_b_mac, 1, 24, 1);
    assert(link_end_b != NULL);
    void interface_free(Interface *arg0);
    Link *link = link_create(link_end_a, link_end_b, 1, 1, 0);
    assert(link != NULL);
    void link_free(Link *arg0);
    Packet * packet_create(size_t capacity);
    Packet *pkt = packet_create(64);
    assert(pkt != NULL);
    void packet_free(Packet *arg0);
    Interface * interface_create(const char * name, const uint8_t * mac, uint32_t ip_addr, uint8_t prefix_len, uint16_t mtu);
    uint8_t src_mac_buf[64];
    memset(src_mac_buf, 0, sizeof(src_mac_buf));
    const uint8_t * src_mac = src_mac_buf;
    Interface *src = interface_create("kleva", src_mac, 1, 24, 1);
    assert(src != NULL);
    void interface_free(Interface *arg0);
    Scheduler * scheduler_create(size_t capacity);
    Scheduler *sched = scheduler_create(64);
    assert(sched != NULL);
    void scheduler_free(Scheduler *arg0);
    EventCallback rx_handler = kleva_stub_EventCallback;
    int out_ret = link_transmit(link, pkt, src, sched, 0, rx_handler, NULL);
    assert(out_ret == -1); /* EVA-proven oracle */
    link_free(link);
    packet_free(pkt);
    interface_free(src);
    scheduler_free(sched);
    printf("PASS test_transmit_valid_tv001\n");
}

static void test_transmit_scheduled_tv001(void) {
    Link * link_create(Interface * end_a, Interface * end_b, uint32_t bw, uint32_t delay, float loss_rate);
    Interface * interface_create(const char * name, const uint8_t * mac, uint32_t ip_addr, uint8_t prefix_len, uint16_t mtu);
    uint8_t link_end_a_mac_buf[64];
    memset(link_end_a_mac_buf, 0, sizeof(link_end_a_mac_buf));
    const uint8_t * link_end_a_mac = link_end_a_mac_buf;
    Interface *link_end_a = interface_create("kleva", link_end_a_mac, 1, 24, 1);
    assert(link_end_a != NULL);
    void interface_free(Interface *arg0);
    Interface * interface_create(const char * name, const uint8_t * mac, uint32_t ip_addr, uint8_t prefix_len, uint16_t mtu);
    uint8_t link_end_b_mac_buf[64];
    memset(link_end_b_mac_buf, 0, sizeof(link_end_b_mac_buf));
    const uint8_t * link_end_b_mac = link_end_b_mac_buf;
    Interface *link_end_b = interface_create("kleva", link_end_b_mac, 1, 24, 1);
    assert(link_end_b != NULL);
    void interface_free(Interface *arg0);
    Link *link = link_create(link_end_a, link_end_b, 1, 1, 0);
    assert(link != NULL);
    void link_free(Link *arg0);
    Packet * packet_create(size_t capacity);
    Packet *pkt = packet_create(64);
    assert(pkt != NULL);
    void packet_free(Packet *arg0);
    Interface * interface_create(const char * name, const uint8_t * mac, uint32_t ip_addr, uint8_t prefix_len, uint16_t mtu);
    uint8_t src_mac_buf[64];
    memset(src_mac_buf, 0, sizeof(src_mac_buf));
    const uint8_t * src_mac = src_mac_buf;
    Interface *src = interface_create("kleva", src_mac, 1, 24, 1);
    assert(src != NULL);
    void interface_free(Interface *arg0);
    Scheduler * scheduler_create(size_t capacity);
    Scheduler *sched = scheduler_create(64);
    assert(sched != NULL);
    void scheduler_free(Scheduler *arg0);
    EventCallback rx_handler = kleva_stub_EventCallback;
    src = link->end_a;
    link->up = ((0) + 1);
    link->end_a->up = ((0) + 1);
    link->end_b->up = ((0) + 1);
    int out_ret = link_transmit(link, pkt, src, sched, 0, rx_handler, NULL);
    assert(out_ret == -1); /* EVA-proven oracle */
    link_free(link);
    packet_free(pkt);
    interface_free(src);
    scheduler_free(sched);
    printf("PASS test_transmit_scheduled_tv001\n");
}

static void test_transmit_ir_if_0_left_left_left_not_link_tv001(void) {
    Link * link_create(Interface * end_a, Interface * end_b, uint32_t bw, uint32_t delay, float loss_rate);
    Interface * interface_create(const char * name, const uint8_t * mac, uint32_t ip_addr, uint8_t prefix_len, uint16_t mtu);
    uint8_t link_end_a_mac_buf[64];
    memset(link_end_a_mac_buf, 0, sizeof(link_end_a_mac_buf));
    const uint8_t * link_end_a_mac = link_end_a_mac_buf;
    Interface *link_end_a = interface_create("kleva", link_end_a_mac, 1, 24, 1);
    assert(link_end_a != NULL);
    void interface_free(Interface *arg0);
    Interface * interface_create(const char * name, const uint8_t * mac, uint32_t ip_addr, uint8_t prefix_len, uint16_t mtu);
    uint8_t link_end_b_mac_buf[64];
    memset(link_end_b_mac_buf, 0, sizeof(link_end_b_mac_buf));
    const uint8_t * link_end_b_mac = link_end_b_mac_buf;
    Interface *link_end_b = interface_create("kleva", link_end_b_mac, 1, 24, 1);
    assert(link_end_b != NULL);
    void interface_free(Interface *arg0);
    Link *link = link_create(link_end_a, link_end_b, 1, 1, 0);
    assert(link != NULL);
    void link_free(Link *arg0);
    Packet * packet_create(size_t capacity);
    Packet *pkt = packet_create(64);
    assert(pkt != NULL);
    void packet_free(Packet *arg0);
    Interface * interface_create(const char * name, const uint8_t * mac, uint32_t ip_addr, uint8_t prefix_len, uint16_t mtu);
    uint8_t src_mac_buf[64];
    memset(src_mac_buf, 0, sizeof(src_mac_buf));
    const uint8_t * src_mac = src_mac_buf;
    Interface *src = interface_create("kleva", src_mac, 1, 24, 1);
    assert(src != NULL);
    void interface_free(Interface *arg0);
    Scheduler * scheduler_create(size_t capacity);
    Scheduler *sched = scheduler_create(64);
    assert(sched != NULL);
    void scheduler_free(Scheduler *arg0);
    EventCallback rx_handler = kleva_stub_EventCallback;
    src = link->end_a;
    link->up = ((0) + 1);
    link->end_a->up = ((0) + 1);
    link->end_b->up = ((0) + 1);
    link = 0;
    int out_ret = link_transmit(link, pkt, src, sched, 0, rx_handler, NULL);
    assert(out_ret == -1); /* EVA-proven oracle */
    link_free(link);
    packet_free(pkt);
    interface_free(src);
    scheduler_free(sched);
    printf("PASS test_transmit_ir_if_0_left_left_left_not_link_tv001\n");
}

static void test_transmit_ir_if_0_left_left_right_not_pkt_tv001(void) {
    Link * link_create(Interface * end_a, Interface * end_b, uint32_t bw, uint32_t delay, float loss_rate);
    Interface * interface_create(const char * name, const uint8_t * mac, uint32_t ip_addr, uint8_t prefix_len, uint16_t mtu);
    uint8_t link_end_a_mac_buf[64];
    memset(link_end_a_mac_buf, 0, sizeof(link_end_a_mac_buf));
    const uint8_t * link_end_a_mac = link_end_a_mac_buf;
    Interface *link_end_a = interface_create("kleva", link_end_a_mac, 1, 24, 1);
    assert(link_end_a != NULL);
    void interface_free(Interface *arg0);
    Interface * interface_create(const char * name, const uint8_t * mac, uint32_t ip_addr, uint8_t prefix_len, uint16_t mtu);
    uint8_t link_end_b_mac_buf[64];
    memset(link_end_b_mac_buf, 0, sizeof(link_end_b_mac_buf));
    const uint8_t * link_end_b_mac = link_end_b_mac_buf;
    Interface *link_end_b = interface_create("kleva", link_end_b_mac, 1, 24, 1);
    assert(link_end_b != NULL);
    void interface_free(Interface *arg0);
    Link *link = link_create(link_end_a, link_end_b, 1, 1, 0);
    assert(link != NULL);
    void link_free(Link *arg0);
    Packet * packet_create(size_t capacity);
    Packet *pkt = packet_create(64);
    assert(pkt != NULL);
    void packet_free(Packet *arg0);
    Interface * interface_create(const char * name, const uint8_t * mac, uint32_t ip_addr, uint8_t prefix_len, uint16_t mtu);
    uint8_t src_mac_buf[64];
    memset(src_mac_buf, 0, sizeof(src_mac_buf));
    const uint8_t * src_mac = src_mac_buf;
    Interface *src = interface_create("kleva", src_mac, 1, 24, 1);
    assert(src != NULL);
    void interface_free(Interface *arg0);
    Scheduler * scheduler_create(size_t capacity);
    Scheduler *sched = scheduler_create(64);
    assert(sched != NULL);
    void scheduler_free(Scheduler *arg0);
    EventCallback rx_handler = kleva_stub_EventCallback;
    src = link->end_a;
    link->up = ((0) + 1);
    link->end_a->up = ((0) + 1);
    link->end_b->up = ((0) + 1);
    pkt = 0;
    int out_ret = link_transmit(link, pkt, src, sched, 0, rx_handler, NULL);
    assert(out_ret == -1); /* EVA-proven oracle */
    link_free(link);
    packet_free(pkt);
    interface_free(src);
    scheduler_free(sched);
    printf("PASS test_transmit_ir_if_0_left_left_right_not_pkt_tv001\n");
}

static void test_transmit_ir_if_0_left_right_not_src_tv001(void) {
    Link * link_create(Interface * end_a, Interface * end_b, uint32_t bw, uint32_t delay, float loss_rate);
    Interface * interface_create(const char * name, const uint8_t * mac, uint32_t ip_addr, uint8_t prefix_len, uint16_t mtu);
    uint8_t link_end_a_mac_buf[64];
    memset(link_end_a_mac_buf, 0, sizeof(link_end_a_mac_buf));
    const uint8_t * link_end_a_mac = link_end_a_mac_buf;
    Interface *link_end_a = interface_create("kleva", link_end_a_mac, 1, 24, 1);
    assert(link_end_a != NULL);
    void interface_free(Interface *arg0);
    Interface * interface_create(const char * name, const uint8_t * mac, uint32_t ip_addr, uint8_t prefix_len, uint16_t mtu);
    uint8_t link_end_b_mac_buf[64];
    memset(link_end_b_mac_buf, 0, sizeof(link_end_b_mac_buf));
    const uint8_t * link_end_b_mac = link_end_b_mac_buf;
    Interface *link_end_b = interface_create("kleva", link_end_b_mac, 1, 24, 1);
    assert(link_end_b != NULL);
    void interface_free(Interface *arg0);
    Link *link = link_create(link_end_a, link_end_b, 1, 1, 0);
    assert(link != NULL);
    void link_free(Link *arg0);
    Packet * packet_create(size_t capacity);
    Packet *pkt = packet_create(64);
    assert(pkt != NULL);
    void packet_free(Packet *arg0);
    Interface * interface_create(const char * name, const uint8_t * mac, uint32_t ip_addr, uint8_t prefix_len, uint16_t mtu);
    uint8_t src_mac_buf[64];
    memset(src_mac_buf, 0, sizeof(src_mac_buf));
    const uint8_t * src_mac = src_mac_buf;
    Interface *src = interface_create("kleva", src_mac, 1, 24, 1);
    assert(src != NULL);
    void interface_free(Interface *arg0);
    Scheduler * scheduler_create(size_t capacity);
    Scheduler *sched = scheduler_create(64);
    assert(sched != NULL);
    void scheduler_free(Scheduler *arg0);
    EventCallback rx_handler = kleva_stub_EventCallback;
    src = link->end_a;
    link->up = ((0) + 1);
    link->end_a->up = ((0) + 1);
    link->end_b->up = ((0) + 1);
    src = 0;
    int out_ret = link_transmit(link, pkt, src, sched, 0, rx_handler, NULL);
    assert(out_ret == -1); /* EVA-proven oracle */
    link_free(link);
    packet_free(pkt);
    interface_free(src);
    scheduler_free(sched);
    printf("PASS test_transmit_ir_if_0_left_right_not_src_tv001\n");
}

static void test_transmit_ir_if_0_right_not_sched_tv001(void) {
    Link * link_create(Interface * end_a, Interface * end_b, uint32_t bw, uint32_t delay, float loss_rate);
    Interface * interface_create(const char * name, const uint8_t * mac, uint32_t ip_addr, uint8_t prefix_len, uint16_t mtu);
    uint8_t link_end_a_mac_buf[64];
    memset(link_end_a_mac_buf, 0, sizeof(link_end_a_mac_buf));
    const uint8_t * link_end_a_mac = link_end_a_mac_buf;
    Interface *link_end_a = interface_create("kleva", link_end_a_mac, 1, 24, 1);
    assert(link_end_a != NULL);
    void interface_free(Interface *arg0);
    Interface * interface_create(const char * name, const uint8_t * mac, uint32_t ip_addr, uint8_t prefix_len, uint16_t mtu);
    uint8_t link_end_b_mac_buf[64];
    memset(link_end_b_mac_buf, 0, sizeof(link_end_b_mac_buf));
    const uint8_t * link_end_b_mac = link_end_b_mac_buf;
    Interface *link_end_b = interface_create("kleva", link_end_b_mac, 1, 24, 1);
    assert(link_end_b != NULL);
    void interface_free(Interface *arg0);
    Link *link = link_create(link_end_a, link_end_b, 1, 1, 0);
    assert(link != NULL);
    void link_free(Link *arg0);
    Packet * packet_create(size_t capacity);
    Packet *pkt = packet_create(64);
    assert(pkt != NULL);
    void packet_free(Packet *arg0);
    Interface * interface_create(const char * name, const uint8_t * mac, uint32_t ip_addr, uint8_t prefix_len, uint16_t mtu);
    uint8_t src_mac_buf[64];
    memset(src_mac_buf, 0, sizeof(src_mac_buf));
    const uint8_t * src_mac = src_mac_buf;
    Interface *src = interface_create("kleva", src_mac, 1, 24, 1);
    assert(src != NULL);
    void interface_free(Interface *arg0);
    Scheduler * scheduler_create(size_t capacity);
    Scheduler *sched = scheduler_create(64);
    assert(sched != NULL);
    void scheduler_free(Scheduler *arg0);
    EventCallback rx_handler = kleva_stub_EventCallback;
    src = link->end_a;
    link->up = ((0) + 1);
    link->end_a->up = ((0) + 1);
    link->end_b->up = ((0) + 1);
    sched = 0;
    int out_ret = link_transmit(link, pkt, src, sched, 0, rx_handler, NULL);
    assert(out_ret == -1); /* EVA-proven oracle */
    link_free(link);
    packet_free(pkt);
    interface_free(src);
    scheduler_free(sched);
    printf("PASS test_transmit_ir_if_0_right_not_sched_tv001\n");
}

static void test_transmit_ir_if_1_not_link_up_tv001(void) {
    Link * link_create(Interface * end_a, Interface * end_b, uint32_t bw, uint32_t delay, float loss_rate);
    Interface * interface_create(const char * name, const uint8_t * mac, uint32_t ip_addr, uint8_t prefix_len, uint16_t mtu);
    uint8_t link_end_a_mac_buf[64];
    memset(link_end_a_mac_buf, 0, sizeof(link_end_a_mac_buf));
    const uint8_t * link_end_a_mac = link_end_a_mac_buf;
    Interface *link_end_a = interface_create("kleva", link_end_a_mac, 1, 24, 1);
    assert(link_end_a != NULL);
    void interface_free(Interface *arg0);
    Interface * interface_create(const char * name, const uint8_t * mac, uint32_t ip_addr, uint8_t prefix_len, uint16_t mtu);
    uint8_t link_end_b_mac_buf[64];
    memset(link_end_b_mac_buf, 0, sizeof(link_end_b_mac_buf));
    const uint8_t * link_end_b_mac = link_end_b_mac_buf;
    Interface *link_end_b = interface_create("kleva", link_end_b_mac, 1, 24, 1);
    assert(link_end_b != NULL);
    void interface_free(Interface *arg0);
    Link *link = link_create(link_end_a, link_end_b, 1, 1, 0);
    assert(link != NULL);
    void link_free(Link *arg0);
    Packet * packet_create(size_t capacity);
    Packet *pkt = packet_create(64);
    assert(pkt != NULL);
    void packet_free(Packet *arg0);
    Interface * interface_create(const char * name, const uint8_t * mac, uint32_t ip_addr, uint8_t prefix_len, uint16_t mtu);
    uint8_t src_mac_buf[64];
    memset(src_mac_buf, 0, sizeof(src_mac_buf));
    const uint8_t * src_mac = src_mac_buf;
    Interface *src = interface_create("kleva", src_mac, 1, 24, 1);
    assert(src != NULL);
    void interface_free(Interface *arg0);
    Scheduler * scheduler_create(size_t capacity);
    Scheduler *sched = scheduler_create(64);
    assert(sched != NULL);
    void scheduler_free(Scheduler *arg0);
    EventCallback rx_handler = kleva_stub_EventCallback;
    src = link->end_a;
    link->up = ((0) + 1);
    link->end_a->up = ((0) + 1);
    link->end_b->up = ((0) + 1);
    assert(link != NULL);
    assert(pkt != NULL);
    assert(src != NULL);
    assert(sched != NULL);
    (link)->up = 0;
    int out_ret = link_transmit(link, pkt, src, sched, 0, rx_handler, NULL);
    assert(out_ret == -1); /* EVA-proven oracle */
    packet_free(pkt);
    interface_free(src);
    scheduler_free(sched);
    printf("PASS test_transmit_ir_if_1_not_link_up_tv001\n");
}

static void test_transmit_ir_if_1_false_not_link_up_tv001(void) {
    Link * link_create(Interface * end_a, Interface * end_b, uint32_t bw, uint32_t delay, float loss_rate);
    Interface * interface_create(const char * name, const uint8_t * mac, uint32_t ip_addr, uint8_t prefix_len, uint16_t mtu);
    uint8_t link_end_a_mac_buf[64];
    memset(link_end_a_mac_buf, 0, sizeof(link_end_a_mac_buf));
    const uint8_t * link_end_a_mac = link_end_a_mac_buf;
    Interface *link_end_a = interface_create("kleva", link_end_a_mac, 1, 24, 1);
    assert(link_end_a != NULL);
    void interface_free(Interface *arg0);
    Interface * interface_create(const char * name, const uint8_t * mac, uint32_t ip_addr, uint8_t prefix_len, uint16_t mtu);
    uint8_t link_end_b_mac_buf[64];
    memset(link_end_b_mac_buf, 0, sizeof(link_end_b_mac_buf));
    const uint8_t * link_end_b_mac = link_end_b_mac_buf;
    Interface *link_end_b = interface_create("kleva", link_end_b_mac, 1, 24, 1);
    assert(link_end_b != NULL);
    void interface_free(Interface *arg0);
    Link *link = link_create(link_end_a, link_end_b, 1, 1, 0);
    assert(link != NULL);
    void link_free(Link *arg0);
    Packet * packet_create(size_t capacity);
    Packet *pkt = packet_create(64);
    assert(pkt != NULL);
    void packet_free(Packet *arg0);
    Interface * interface_create(const char * name, const uint8_t * mac, uint32_t ip_addr, uint8_t prefix_len, uint16_t mtu);
    uint8_t src_mac_buf[64];
    memset(src_mac_buf, 0, sizeof(src_mac_buf));
    const uint8_t * src_mac = src_mac_buf;
    Interface *src = interface_create("kleva", src_mac, 1, 24, 1);
    assert(src != NULL);
    void interface_free(Interface *arg0);
    Scheduler * scheduler_create(size_t capacity);
    Scheduler *sched = scheduler_create(64);
    assert(sched != NULL);
    void scheduler_free(Scheduler *arg0);
    EventCallback rx_handler = kleva_stub_EventCallback;
    src = link->end_a;
    link->up = ((0) + 1);
    link->end_a->up = ((0) + 1);
    link->end_b->up = ((0) + 1);
    assert(link != NULL);
    assert(pkt != NULL);
    assert(src != NULL);
    assert(sched != NULL);
    (link)->up = 1;
    int out_ret = link_transmit(link, pkt, src, sched, 0, rx_handler, NULL);
    assert(out_ret == -1); /* EVA-proven oracle */
    packet_free(pkt);
    interface_free(src);
    scheduler_free(sched);
    printf("PASS test_transmit_ir_if_1_false_not_link_up_tv001\n");
}

static void test_transmit_ir_callee_packet_clone_zero_failure_tv001(void) {
    Link * link_create(Interface * end_a, Interface * end_b, uint32_t bw, uint32_t delay, float loss_rate);
    Interface * interface_create(const char * name, const uint8_t * mac, uint32_t ip_addr, uint8_t prefix_len, uint16_t mtu);
    uint8_t link_end_a_mac_buf[64];
    memset(link_end_a_mac_buf, 0, sizeof(link_end_a_mac_buf));
    const uint8_t * link_end_a_mac = link_end_a_mac_buf;
    Interface *link_end_a = interface_create("kleva", link_end_a_mac, 1, 24, 1);
    assert(link_end_a != NULL);
    void interface_free(Interface *arg0);
    Interface * interface_create(const char * name, const uint8_t * mac, uint32_t ip_addr, uint8_t prefix_len, uint16_t mtu);
    uint8_t link_end_b_mac_buf[64];
    memset(link_end_b_mac_buf, 0, sizeof(link_end_b_mac_buf));
    const uint8_t * link_end_b_mac = link_end_b_mac_buf;
    Interface *link_end_b = interface_create("kleva", link_end_b_mac, 1, 24, 1);
    assert(link_end_b != NULL);
    void interface_free(Interface *arg0);
    Link *link = link_create(link_end_a, link_end_b, 1, 1, 0);
    assert(link != NULL);
    void link_free(Link *arg0);
    Packet * packet_create(size_t capacity);
    Packet *pkt = packet_create(64);
    assert(pkt != NULL);
    void packet_free(Packet *arg0);
    Interface * interface_create(const char * name, const uint8_t * mac, uint32_t ip_addr, uint8_t prefix_len, uint16_t mtu);
    uint8_t src_mac_buf[64];
    memset(src_mac_buf, 0, sizeof(src_mac_buf));
    const uint8_t * src_mac = src_mac_buf;
    Interface *src = interface_create("kleva", src_mac, 1, 24, 1);
    assert(src != NULL);
    void interface_free(Interface *arg0);
    Scheduler * scheduler_create(size_t capacity);
    Scheduler *sched = scheduler_create(64);
    assert(sched != NULL);
    void scheduler_free(Scheduler *arg0);
    EventCallback rx_handler = kleva_stub_EventCallback;
    src = link->end_a;
    link->up = ((0) + 1);
    link->end_a->up = ((0) + 1);
    link->end_b->up = ((0) + 1);
    int out_ret = link_transmit(link, pkt, src, sched, 0, rx_handler, NULL);
    assert(out_ret == -1); /* EVA-proven oracle */
    link_free(link);
    packet_free(pkt);
    interface_free(src);
    scheduler_free(sched);
    printf("PASS test_transmit_ir_callee_packet_clone_zero_failure_tv001\n");
}

static void test_transmit_ir_callee_event_create_callback_zero_failure_tv001(void) {
    __kleva_alloc_reset();
    Link * link_create(Interface * end_a, Interface * end_b, uint32_t bw, uint32_t delay, float loss_rate);
    Interface * interface_create(const char * name, const uint8_t * mac, uint32_t ip_addr, uint8_t prefix_len, uint16_t mtu);
    uint8_t link_end_a_mac_buf[64];
    memset(link_end_a_mac_buf, 0, sizeof(link_end_a_mac_buf));
    const uint8_t * link_end_a_mac = link_end_a_mac_buf;
    Interface *link_end_a = interface_create("kleva", link_end_a_mac, 1, 24, 1);
    assert(link_end_a != NULL);
    void interface_free(Interface *arg0);
    Interface * interface_create(const char * name, const uint8_t * mac, uint32_t ip_addr, uint8_t prefix_len, uint16_t mtu);
    uint8_t link_end_b_mac_buf[64];
    memset(link_end_b_mac_buf, 0, sizeof(link_end_b_mac_buf));
    const uint8_t * link_end_b_mac = link_end_b_mac_buf;
    Interface *link_end_b = interface_create("kleva", link_end_b_mac, 1, 24, 1);
    assert(link_end_b != NULL);
    void interface_free(Interface *arg0);
    Link *link = link_create(link_end_a, link_end_b, 1, 1, 0);
    assert(link != NULL);
    void link_free(Link *arg0);
    Packet * packet_create(size_t capacity);
    Packet *pkt = packet_create(64);
    assert(pkt != NULL);
    void packet_free(Packet *arg0);
    Interface * interface_create(const char * name, const uint8_t * mac, uint32_t ip_addr, uint8_t prefix_len, uint16_t mtu);
    uint8_t src_mac_buf[64];
    memset(src_mac_buf, 0, sizeof(src_mac_buf));
    const uint8_t * src_mac = src_mac_buf;
    Interface *src = interface_create("kleva", src_mac, 1, 24, 1);
    assert(src != NULL);
    void interface_free(Interface *arg0);
    Scheduler * scheduler_create(size_t capacity);
    Scheduler *sched = scheduler_create(64);
    assert(sched != NULL);
    void scheduler_free(Scheduler *arg0);
    EventCallback rx_handler = kleva_stub_EventCallback;
    src = link->end_a;
    link->up = ((0) + 1);
    link->end_a->up = ((0) + 1);
    link->end_b->up = ((0) + 1);
    __kleva_alloc_fail_on(0);
    int out_ret = link_transmit(link, pkt, src, sched, 0, rx_handler, NULL);
    assert(out_ret == -1); /* EVA-proven oracle */
    link_free(link);
    packet_free(pkt);
    interface_free(src);
    scheduler_free(sched);
    printf("PASS test_transmit_ir_callee_event_create_callback_zero_failure_tv001\n");
}

static void test_transmit_ir_diversity_now_min_tv001(void) {
    Link * link_create(Interface * end_a, Interface * end_b, uint32_t bw, uint32_t delay, float loss_rate);
    Interface * interface_create(const char * name, const uint8_t * mac, uint32_t ip_addr, uint8_t prefix_len, uint16_t mtu);
    uint8_t link_end_a_mac_buf[64];
    memset(link_end_a_mac_buf, 0, sizeof(link_end_a_mac_buf));
    const uint8_t * link_end_a_mac = link_end_a_mac_buf;
    Interface *link_end_a = interface_create("kleva", link_end_a_mac, 1, 24, 1);
    assert(link_end_a != NULL);
    void interface_free(Interface *arg0);
    Interface * interface_create(const char * name, const uint8_t * mac, uint32_t ip_addr, uint8_t prefix_len, uint16_t mtu);
    uint8_t link_end_b_mac_buf[64];
    memset(link_end_b_mac_buf, 0, sizeof(link_end_b_mac_buf));
    const uint8_t * link_end_b_mac = link_end_b_mac_buf;
    Interface *link_end_b = interface_create("kleva", link_end_b_mac, 1, 24, 1);
    assert(link_end_b != NULL);
    void interface_free(Interface *arg0);
    Link *link = link_create(link_end_a, link_end_b, 1, 1, 0);
    assert(link != NULL);
    void link_free(Link *arg0);
    Packet * packet_create(size_t capacity);
    Packet *pkt = packet_create(64);
    assert(pkt != NULL);
    void packet_free(Packet *arg0);
    Interface * interface_create(const char * name, const uint8_t * mac, uint32_t ip_addr, uint8_t prefix_len, uint16_t mtu);
    uint8_t src_mac_buf[64];
    memset(src_mac_buf, 0, sizeof(src_mac_buf));
    const uint8_t * src_mac = src_mac_buf;
    Interface *src = interface_create("kleva", src_mac, 1, 24, 1);
    assert(src != NULL);
    void interface_free(Interface *arg0);
    Scheduler * scheduler_create(size_t capacity);
    Scheduler *sched = scheduler_create(64);
    assert(sched != NULL);
    void scheduler_free(Scheduler *arg0);
    EventCallback rx_handler = kleva_stub_EventCallback;
    src = link->end_a;
    link->up = ((0) + 1);
    link->end_a->up = ((0) + 1);
    link->end_b->up = ((0) + 1);
    int out_ret = link_transmit(link, pkt, src, sched, 0, rx_handler, NULL);
    assert(out_ret == -1); /* EVA-proven oracle */
    link_free(link);
    packet_free(pkt);
    interface_free(src);
    scheduler_free(sched);
    printf("PASS test_transmit_ir_diversity_now_min_tv001\n");
}

static void test_transmit_ir_diversity_now_one_tv001(void) {
    Link * link_create(Interface * end_a, Interface * end_b, uint32_t bw, uint32_t delay, float loss_rate);
    Interface * interface_create(const char * name, const uint8_t * mac, uint32_t ip_addr, uint8_t prefix_len, uint16_t mtu);
    uint8_t link_end_a_mac_buf[64];
    memset(link_end_a_mac_buf, 0, sizeof(link_end_a_mac_buf));
    const uint8_t * link_end_a_mac = link_end_a_mac_buf;
    Interface *link_end_a = interface_create("kleva", link_end_a_mac, 1, 24, 1);
    assert(link_end_a != NULL);
    void interface_free(Interface *arg0);
    Interface * interface_create(const char * name, const uint8_t * mac, uint32_t ip_addr, uint8_t prefix_len, uint16_t mtu);
    uint8_t link_end_b_mac_buf[64];
    memset(link_end_b_mac_buf, 0, sizeof(link_end_b_mac_buf));
    const uint8_t * link_end_b_mac = link_end_b_mac_buf;
    Interface *link_end_b = interface_create("kleva", link_end_b_mac, 1, 24, 1);
    assert(link_end_b != NULL);
    void interface_free(Interface *arg0);
    Link *link = link_create(link_end_a, link_end_b, 1, 1, 0);
    assert(link != NULL);
    void link_free(Link *arg0);
    Packet * packet_create(size_t capacity);
    Packet *pkt = packet_create(64);
    assert(pkt != NULL);
    void packet_free(Packet *arg0);
    Interface * interface_create(const char * name, const uint8_t * mac, uint32_t ip_addr, uint8_t prefix_len, uint16_t mtu);
    uint8_t src_mac_buf[64];
    memset(src_mac_buf, 0, sizeof(src_mac_buf));
    const uint8_t * src_mac = src_mac_buf;
    Interface *src = interface_create("kleva", src_mac, 1, 24, 1);
    assert(src != NULL);
    void interface_free(Interface *arg0);
    Scheduler * scheduler_create(size_t capacity);
    Scheduler *sched = scheduler_create(64);
    assert(sched != NULL);
    void scheduler_free(Scheduler *arg0);
    EventCallback rx_handler = kleva_stub_EventCallback;
    src = link->end_a;
    link->up = ((0) + 1);
    link->end_a->up = ((0) + 1);
    link->end_b->up = ((0) + 1);
    int out_ret = link_transmit(link, pkt, src, sched, 1, rx_handler, NULL);
    assert(out_ret == -1); /* EVA-proven oracle */
    link_free(link);
    packet_free(pkt);
    interface_free(src);
    scheduler_free(sched);
    printf("PASS test_transmit_ir_diversity_now_one_tv001\n");
}

int main(void) {
    __kleva_alloc_reset();
    test_create_null_or_bad_tv001();
    __kleva_alloc_reset();
    test_create_ok_tv001();
    __kleva_alloc_reset();
    test_create_ir_if_0_left_left_not_end_a_tv001();
    __kleva_alloc_reset();
    test_create_ir_if_0_left_right_not_end_b_tv001();
    __kleva_alloc_reset();
    test_create_ir_if_0_right_bw_eq_0_tv001();
    __kleva_alloc_reset();
    test_create_ir_if_0_false_false_not_end_a_and_false_not_end_b_and_false_bw_ne_0_tv001();
    __kleva_alloc_reset();
    test_create_ir_alloc_malloc_0_zero_failure_tv001();
    __kleva_alloc_reset();
    test_create_ir_forbidden_value_0_bw_eq_0_forbidden_tv001();
    __kleva_alloc_reset();
    test_create_ir_forbidden_value_0_bw_eq_0_allowed_tv001();
    __kleva_alloc_reset();
    test_create_ir_diversity_bw_one_tv001();
    __kleva_alloc_reset();
    test_create_ir_diversity_delay_min_tv001();
    __kleva_alloc_reset();
    test_create_ir_diversity_delay_one_tv001();
    __kleva_alloc_reset();
    test_free_valid_tv001();
    __kleva_alloc_reset();
    test_set_up_null_tv001();
    __kleva_alloc_reset();
    test_set_up_valid_tv001();
    __kleva_alloc_reset();
    test_set_up_ir_if_0_truthy_link_tv001();
    __kleva_alloc_reset();
    test_set_up_ir_if_0_false_truthy_link_tv001();
    __kleva_alloc_reset();
    test_set_up_ir_diversity_up_min_tv001();
    __kleva_alloc_reset();
    test_set_up_ir_diversity_up_one_tv001();
    __kleva_alloc_reset();
    test_is_up_null_tv001();
    __kleva_alloc_reset();
    test_is_up_valid_tv001();
    __kleva_alloc_reset();
    test_is_up_ir_if_0_truthy_link_tv001();
    __kleva_alloc_reset();
    test_is_up_ir_if_0_false_truthy_link_tv001();
    __kleva_alloc_reset();
    test_get_other_interface_null_tv001();
    __kleva_alloc_reset();
    test_get_other_interface_mismatch_tv001();
    __kleva_alloc_reset();
    test_get_other_interface_valid_tv001();
    __kleva_alloc_reset();
    test_get_other_interface_ir_if_0_left_not_link_tv001();
    __kleva_alloc_reset();
    test_get_other_interface_ir_if_0_right_not_src_tv001();
    __kleva_alloc_reset();
    test_transmit_null_tv001();
    __kleva_alloc_reset();
    test_transmit_valid_tv001();
    __kleva_alloc_reset();
    test_transmit_scheduled_tv001();
    __kleva_alloc_reset();
    test_transmit_ir_if_0_left_left_left_not_link_tv001();
    __kleva_alloc_reset();
    test_transmit_ir_if_0_left_left_right_not_pkt_tv001();
    __kleva_alloc_reset();
    test_transmit_ir_if_0_left_right_not_src_tv001();
    __kleva_alloc_reset();
    test_transmit_ir_if_0_right_not_sched_tv001();
    __kleva_alloc_reset();
    test_transmit_ir_if_1_not_link_up_tv001();
    __kleva_alloc_reset();
    test_transmit_ir_if_1_false_not_link_up_tv001();
    __kleva_alloc_reset();
    test_transmit_ir_callee_packet_clone_zero_failure_tv001();
    __kleva_alloc_reset();
    test_transmit_ir_callee_event_create_callback_zero_failure_tv001();
    __kleva_alloc_reset();
    test_transmit_ir_diversity_now_min_tv001();
    __kleva_alloc_reset();
    test_transmit_ir_diversity_now_one_tv001();

    printf("\nAll unit tests passed.\n");
    return 0;
}
