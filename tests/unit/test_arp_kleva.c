/* AUTO-GENERATED by kleva (KLEE + EVA) — DO NOT EDIT
 * Oracle values are EVA-proven singletons — not hand-written expected values.
 * Re-generate: kleva gen <module.yaml>
 * Generated: 2026-07-02 18:04:09
 */
#include <assert.h>
#include <stdio.h>
#include <string.h>
#include <stdint.h>
#include "arp.h"

#include "arp.h"
#include "scheduler.h"
#include "topology.h"
#include "simulator.h"
#include "interface.h"
#include "packet.h"

static void test_init_null_tv001(void) {
    arp_init(NULL);
    int out_call_completed = 1;
    assert(out_call_completed == 1); /* EVA-proven oracle */
    printf("PASS test_init_null_tv001\n");
}

static void test_init_valid_tv001(void) {
    Simulator * simulator_create(Topology * topo, Scheduler * sched);
    Topology * topology_create(void);
    Topology *sim_topo = topology_create();
    assert(sim_topo != NULL);
    void topology_free(Topology *arg0);
    Scheduler * scheduler_create(size_t capacity);
    Scheduler *sim_sched = scheduler_create(64);
    assert(sim_sched != NULL);
    void scheduler_free(Scheduler *arg0);
    Simulator *sim = simulator_create(sim_topo, sim_sched);
    assert(sim != NULL);
    void simulator_free(Simulator *arg0);
    arp_init(sim);
    int out_sim_topo_nonnull = (sim->topo != NULL);
    int out_sim_sched_nonnull = (sim->sched != NULL);
    uint64_t out_sim_end_time = sim->end_time;
    /* oracle-deferred: post-call witness will be emitted below */
    assert(out_sim_topo_nonnull == 1); /* EVA-proven oracle */
    assert(out_sim_sched_nonnull == 1); /* EVA-proven oracle */
    assert(out_sim_end_time == 0); /* EVA-proven oracle */
    printf("PASS test_init_valid_tv001\n");
}

static void test_init_ir_if_0_not_sim_tv001(void) {
    Simulator * simulator_create(Topology * topo, Scheduler * sched);
    Topology * topology_create(void);
    Topology *sim_topo = topology_create();
    assert(sim_topo != NULL);
    void topology_free(Topology *arg0);
    Scheduler * scheduler_create(size_t capacity);
    Scheduler *sim_sched = scheduler_create(64);
    assert(sim_sched != NULL);
    void scheduler_free(Scheduler *arg0);
    Simulator *sim = simulator_create(sim_topo, sim_sched);
    assert(sim != NULL);
    void simulator_free(Simulator *arg0);
    sim = 0;
    arp_init(sim);
    int out_call_completed = 1;
    assert(out_call_completed == 1); /* EVA-proven oracle */
    printf("PASS test_init_ir_if_0_not_sim_tv001\n");
}

static void test_init_ir_if_0_false_not_sim_tv001(void) {
    Simulator * simulator_create(Topology * topo, Scheduler * sched);
    Topology * topology_create(void);
    Topology *sim_topo = topology_create();
    assert(sim_topo != NULL);
    void topology_free(Topology *arg0);
    Scheduler * scheduler_create(size_t capacity);
    Scheduler *sim_sched = scheduler_create(64);
    assert(sim_sched != NULL);
    void scheduler_free(Scheduler *arg0);
    Simulator *sim = simulator_create(sim_topo, sim_sched);
    assert(sim != NULL);
    void simulator_free(Simulator *arg0);
    void *malloc(size_t size);
    Scheduler *sim_sched_0 = malloc(sizeof(*sim_sched_0));
    if (!sim_sched_0) return 0;
    memset(sim_sched_0, 0, sizeof(*sim_sched_0));
    sim->sched = sim_sched_0;
    assert(sim != NULL);
    arp_init(sim);
    int out_call_completed = 1;
    assert(out_call_completed == 1); /* EVA-proven oracle */
    printf("PASS test_init_ir_if_0_false_not_sim_tv001\n");
}

static void test_send_request_null_tv001(void) {
    int out_ret = arp_send_request(NULL, NULL, 0);
    int out_sentinel = (out_ret == -1) ? 1 : 0;
    assert(out_ret == -1); /* EVA-proven oracle */
    assert(out_sentinel == 1); /* EVA-proven oracle */
    printf("PASS test_send_request_null_tv001\n");
}

static void test_send_request_valid_tv001(void) {
    Simulator * simulator_create(Topology * topo, Scheduler * sched);
    Topology * topology_create(void);
    Topology *sim_topo = topology_create();
    assert(sim_topo != NULL);
    void topology_free(Topology *arg0);
    Scheduler * scheduler_create(size_t capacity);
    Scheduler *sim_sched = scheduler_create(64);
    assert(sim_sched != NULL);
    void scheduler_free(Scheduler *arg0);
    Simulator *sim = simulator_create(sim_topo, sim_sched);
    assert(sim != NULL);
    void simulator_free(Simulator *arg0);
    Interface * interface_create(const char * name, const uint8_t * mac, uint32_t ip_addr, uint8_t prefix_len, uint16_t mtu);
    uint8_t iface_mac_buf[64];
    memset(iface_mac_buf, 0, sizeof(iface_mac_buf));
    const uint8_t * iface_mac = iface_mac_buf;
    Interface *iface = interface_create("kleva", iface_mac, 1, 24, 1);
    assert(iface != NULL);
    void interface_free(Interface *arg0);
    int out_ret = arp_send_request(sim, iface, 1);
    assert(out_ret == -1); /* EVA-proven oracle */
    simulator_free(sim);
    interface_free(iface);
    printf("PASS test_send_request_valid_tv001\n");
}

static void test_send_request_ir_if_0_left_left_not_sim_tv001(void) {
    Simulator * simulator_create(Topology * topo, Scheduler * sched);
    Topology * topology_create(void);
    Topology *sim_topo = topology_create();
    assert(sim_topo != NULL);
    void topology_free(Topology *arg0);
    Scheduler * scheduler_create(size_t capacity);
    Scheduler *sim_sched = scheduler_create(64);
    assert(sim_sched != NULL);
    void scheduler_free(Scheduler *arg0);
    Simulator *sim = simulator_create(sim_topo, sim_sched);
    assert(sim != NULL);
    void simulator_free(Simulator *arg0);
    Interface * interface_create(const char * name, const uint8_t * mac, uint32_t ip_addr, uint8_t prefix_len, uint16_t mtu);
    uint8_t iface_mac_buf[64];
    memset(iface_mac_buf, 0, sizeof(iface_mac_buf));
    const uint8_t * iface_mac = iface_mac_buf;
    Interface *iface = interface_create("kleva", iface_mac, 1, 24, 1);
    assert(iface != NULL);
    void interface_free(Interface *arg0);
    sim = 0;
    int out_ret = arp_send_request(sim, iface, 1);
    assert(out_ret == -1); /* EVA-proven oracle */
    simulator_free(sim);
    interface_free(iface);
    printf("PASS test_send_request_ir_if_0_left_left_not_sim_tv001\n");
}

static void test_send_request_ir_if_0_left_right_not_iface_tv001(void) {
    Simulator * simulator_create(Topology * topo, Scheduler * sched);
    Topology * topology_create(void);
    Topology *sim_topo = topology_create();
    assert(sim_topo != NULL);
    void topology_free(Topology *arg0);
    Scheduler * scheduler_create(size_t capacity);
    Scheduler *sim_sched = scheduler_create(64);
    assert(sim_sched != NULL);
    void scheduler_free(Scheduler *arg0);
    Simulator *sim = simulator_create(sim_topo, sim_sched);
    assert(sim != NULL);
    void simulator_free(Simulator *arg0);
    Interface * interface_create(const char * name, const uint8_t * mac, uint32_t ip_addr, uint8_t prefix_len, uint16_t mtu);
    uint8_t iface_mac_buf[64];
    memset(iface_mac_buf, 0, sizeof(iface_mac_buf));
    const uint8_t * iface_mac = iface_mac_buf;
    Interface *iface = interface_create("kleva", iface_mac, 1, 24, 1);
    assert(iface != NULL);
    void interface_free(Interface *arg0);
    iface = 0;
    int out_ret = arp_send_request(sim, iface, 1);
    assert(out_ret == -1); /* EVA-proven oracle */
    simulator_free(sim);
    interface_free(iface);
    printf("PASS test_send_request_ir_if_0_left_right_not_iface_tv001\n");
}

static void test_send_request_ir_if_0_right_not_target_ip_tv001(void) {
    uint32_t target_ip = (uint32_t)0ULL;
    Simulator * simulator_create(Topology * topo, Scheduler * sched);
    Topology * topology_create(void);
    Topology *sim_topo = topology_create();
    assert(sim_topo != NULL);
    void topology_free(Topology *arg0);
    Scheduler * scheduler_create(size_t capacity);
    Scheduler *sim_sched = scheduler_create(64);
    assert(sim_sched != NULL);
    void scheduler_free(Scheduler *arg0);
    Simulator *sim = simulator_create(sim_topo, sim_sched);
    assert(sim != NULL);
    void simulator_free(Simulator *arg0);
    Interface * interface_create(const char * name, const uint8_t * mac, uint32_t ip_addr, uint8_t prefix_len, uint16_t mtu);
    uint8_t iface_mac_buf[64];
    memset(iface_mac_buf, 0, sizeof(iface_mac_buf));
    const uint8_t * iface_mac = iface_mac_buf;
    Interface *iface = interface_create("kleva", iface_mac, 1, 24, 1);
    assert(iface != NULL);
    void interface_free(Interface *arg0);
    target_ip = 0;
    int out_ret = arp_send_request(sim, iface, target_ip);
    assert(out_ret == -1); /* EVA-proven oracle */
    simulator_free(sim);
    interface_free(iface);
    printf("PASS test_send_request_ir_if_0_right_not_target_ip_tv001\n");
}

static void test_send_request_ir_if_0_false_false_not_sim_and_false_not_iface_and_false_not_target_ip_tv001(void) {
    uint32_t target_ip = (uint32_t)0ULL;
    Simulator * simulator_create(Topology * topo, Scheduler * sched);
    Topology * topology_create(void);
    Topology *sim_topo = topology_create();
    assert(sim_topo != NULL);
    void topology_free(Topology *arg0);
    Scheduler * scheduler_create(size_t capacity);
    Scheduler *sim_sched = scheduler_create(64);
    assert(sim_sched != NULL);
    void scheduler_free(Scheduler *arg0);
    Simulator *sim = simulator_create(sim_topo, sim_sched);
    assert(sim != NULL);
    void simulator_free(Simulator *arg0);
    Interface * interface_create(const char * name, const uint8_t * mac, uint32_t ip_addr, uint8_t prefix_len, uint16_t mtu);
    uint8_t iface_mac_buf[64];
    memset(iface_mac_buf, 0, sizeof(iface_mac_buf));
    const uint8_t * iface_mac = iface_mac_buf;
    Interface *iface = interface_create("kleva", iface_mac, 1, 24, 1);
    assert(iface != NULL);
    void interface_free(Interface *arg0);
    assert(sim != NULL);
    assert(iface != NULL);
    target_ip = 1;
    int out_ret = arp_send_request(sim, iface, target_ip);
    assert(out_ret == -1); /* EVA-proven oracle */
    simulator_free(sim);
    printf("PASS test_send_request_ir_if_0_false_false_not_sim_and_false_not_iface_and_false_not_target_ip_tv001\n");
}

static void test_send_request_ir_diversity_target_ip_one_tv001(void) {
    Simulator * simulator_create(Topology * topo, Scheduler * sched);
    Topology * topology_create(void);
    Topology *sim_topo = topology_create();
    assert(sim_topo != NULL);
    void topology_free(Topology *arg0);
    Scheduler * scheduler_create(size_t capacity);
    Scheduler *sim_sched = scheduler_create(64);
    assert(sim_sched != NULL);
    void scheduler_free(Scheduler *arg0);
    Simulator *sim = simulator_create(sim_topo, sim_sched);
    assert(sim != NULL);
    void simulator_free(Simulator *arg0);
    Interface * interface_create(const char * name, const uint8_t * mac, uint32_t ip_addr, uint8_t prefix_len, uint16_t mtu);
    uint8_t iface_mac_buf[64];
    memset(iface_mac_buf, 0, sizeof(iface_mac_buf));
    const uint8_t * iface_mac = iface_mac_buf;
    Interface *iface = interface_create("kleva", iface_mac, 1, 24, 1);
    assert(iface != NULL);
    void interface_free(Interface *arg0);
    int out_ret = arp_send_request(sim, iface, 1);
    assert(out_ret == -1); /* EVA-proven oracle */
    simulator_free(sim);
    interface_free(iface);
    printf("PASS test_send_request_ir_diversity_target_ip_one_tv001\n");
}

static void test_send_reply_null_tv001(void) {
    int out_ret = arp_send_reply(NULL, NULL, NULL);
    int out_sentinel = (out_ret == -1) ? 1 : 0;
    assert(out_ret == -1); /* EVA-proven oracle */
    assert(out_sentinel == 1); /* EVA-proven oracle */
    printf("PASS test_send_reply_null_tv001\n");
}

static void test_send_reply_valid_tv001(void) {
    Simulator * simulator_create(Topology * topo, Scheduler * sched);
    Topology * topology_create(void);
    Topology *sim_topo = topology_create();
    assert(sim_topo != NULL);
    void topology_free(Topology *arg0);
    Scheduler * scheduler_create(size_t capacity);
    Scheduler *sim_sched = scheduler_create(64);
    assert(sim_sched != NULL);
    void scheduler_free(Scheduler *arg0);
    Simulator *sim = simulator_create(sim_topo, sim_sched);
    assert(sim != NULL);
    void simulator_free(Simulator *arg0);
    Interface * interface_create(const char * name, const uint8_t * mac, uint32_t ip_addr, uint8_t prefix_len, uint16_t mtu);
    uint8_t iface_mac_buf[64];
    memset(iface_mac_buf, 0, sizeof(iface_mac_buf));
    const uint8_t * iface_mac = iface_mac_buf;
    Interface *iface = interface_create("kleva", iface_mac, 1, 24, 1);
    assert(iface != NULL);
    void interface_free(Interface *arg0);
    Packet * packet_create(size_t capacity);
    Packet *req_pkt = packet_create(64);
    assert(req_pkt != NULL);
    void packet_free(Packet *arg0);
    uint8_t req_pkt_data[64];
    memset(req_pkt_data, 0, sizeof(req_pkt_data));
    if (req_pkt->data == NULL) req_pkt->data = req_pkt_data;
    if (req_pkt->len == 0) req_pkt->len = 8;
    memset(req_pkt->data, 0, req_pkt->len);
    req_pkt->len = 28;
    uint8_t req_pkt_data_buffer[64];
    memset(req_pkt_data_buffer, 0, sizeof(req_pkt_data_buffer));
    if (req_pkt->data == NULL) req_pkt->data = req_pkt_data_buffer;
    memset(req_pkt->data, 0, req_pkt->len);
    int out_ret = arp_send_reply(sim, iface, req_pkt);
    assert(out_ret == -1); /* EVA-proven oracle */
    simulator_free(sim);
    interface_free(iface);
    printf("PASS test_send_reply_valid_tv001\n");
}

static void test_send_reply_ir_if_0_left_left_not_sim_tv001(void) {
    Simulator * simulator_create(Topology * topo, Scheduler * sched);
    Topology * topology_create(void);
    Topology *sim_topo = topology_create();
    assert(sim_topo != NULL);
    void topology_free(Topology *arg0);
    Scheduler * scheduler_create(size_t capacity);
    Scheduler *sim_sched = scheduler_create(64);
    assert(sim_sched != NULL);
    void scheduler_free(Scheduler *arg0);
    Simulator *sim = simulator_create(sim_topo, sim_sched);
    assert(sim != NULL);
    void simulator_free(Simulator *arg0);
    Interface * interface_create(const char * name, const uint8_t * mac, uint32_t ip_addr, uint8_t prefix_len, uint16_t mtu);
    uint8_t iface_mac_buf[64];
    memset(iface_mac_buf, 0, sizeof(iface_mac_buf));
    const uint8_t * iface_mac = iface_mac_buf;
    Interface *iface = interface_create("kleva", iface_mac, 1, 24, 1);
    assert(iface != NULL);
    void interface_free(Interface *arg0);
    Packet * packet_create(size_t capacity);
    Packet *req_pkt = packet_create(64);
    assert(req_pkt != NULL);
    void packet_free(Packet *arg0);
    uint8_t req_pkt_data[64];
    memset(req_pkt_data, 0, sizeof(req_pkt_data));
    if (req_pkt->data == NULL) req_pkt->data = req_pkt_data;
    if (req_pkt->len == 0) req_pkt->len = 8;
    memset(req_pkt->data, 0, req_pkt->len);
    req_pkt->len = 28;
    sim = 0;
    uint8_t req_pkt_data_buffer[64];
    memset(req_pkt_data_buffer, 0, sizeof(req_pkt_data_buffer));
    if (req_pkt->data == NULL) req_pkt->data = req_pkt_data_buffer;
    memset(req_pkt->data, 0, req_pkt->len);
    int out_ret = arp_send_reply(sim, iface, req_pkt);
    assert(out_ret == -1); /* EVA-proven oracle */
    simulator_free(sim);
    interface_free(iface);
    printf("PASS test_send_reply_ir_if_0_left_left_not_sim_tv001\n");
}

static void test_send_reply_ir_if_0_left_right_not_iface_tv001(void) {
    Simulator * simulator_create(Topology * topo, Scheduler * sched);
    Topology * topology_create(void);
    Topology *sim_topo = topology_create();
    assert(sim_topo != NULL);
    void topology_free(Topology *arg0);
    Scheduler * scheduler_create(size_t capacity);
    Scheduler *sim_sched = scheduler_create(64);
    assert(sim_sched != NULL);
    void scheduler_free(Scheduler *arg0);
    Simulator *sim = simulator_create(sim_topo, sim_sched);
    assert(sim != NULL);
    void simulator_free(Simulator *arg0);
    Interface * interface_create(const char * name, const uint8_t * mac, uint32_t ip_addr, uint8_t prefix_len, uint16_t mtu);
    uint8_t iface_mac_buf[64];
    memset(iface_mac_buf, 0, sizeof(iface_mac_buf));
    const uint8_t * iface_mac = iface_mac_buf;
    Interface *iface = interface_create("kleva", iface_mac, 1, 24, 1);
    assert(iface != NULL);
    void interface_free(Interface *arg0);
    Packet * packet_create(size_t capacity);
    Packet *req_pkt = packet_create(64);
    assert(req_pkt != NULL);
    void packet_free(Packet *arg0);
    uint8_t req_pkt_data[64];
    memset(req_pkt_data, 0, sizeof(req_pkt_data));
    if (req_pkt->data == NULL) req_pkt->data = req_pkt_data;
    if (req_pkt->len == 0) req_pkt->len = 8;
    memset(req_pkt->data, 0, req_pkt->len);
    req_pkt->len = 28;
    iface = 0;
    uint8_t req_pkt_data_buffer[64];
    memset(req_pkt_data_buffer, 0, sizeof(req_pkt_data_buffer));
    if (req_pkt->data == NULL) req_pkt->data = req_pkt_data_buffer;
    memset(req_pkt->data, 0, req_pkt->len);
    int out_ret = arp_send_reply(sim, iface, req_pkt);
    assert(out_ret == -1); /* EVA-proven oracle */
    simulator_free(sim);
    interface_free(iface);
    printf("PASS test_send_reply_ir_if_0_left_right_not_iface_tv001\n");
}

static void test_send_reply_ir_if_0_false_false_not_sim_and_false_not_iface_and_false_not_req_pkt_tv001(void) {
    Simulator * simulator_create(Topology * topo, Scheduler * sched);
    Topology * topology_create(void);
    Topology *sim_topo = topology_create();
    assert(sim_topo != NULL);
    void topology_free(Topology *arg0);
    Scheduler * scheduler_create(size_t capacity);
    Scheduler *sim_sched = scheduler_create(64);
    assert(sim_sched != NULL);
    void scheduler_free(Scheduler *arg0);
    Simulator *sim = simulator_create(sim_topo, sim_sched);
    assert(sim != NULL);
    void simulator_free(Simulator *arg0);
    Interface * interface_create(const char * name, const uint8_t * mac, uint32_t ip_addr, uint8_t prefix_len, uint16_t mtu);
    uint8_t iface_mac_buf[64];
    memset(iface_mac_buf, 0, sizeof(iface_mac_buf));
    const uint8_t * iface_mac = iface_mac_buf;
    Interface *iface = interface_create("kleva", iface_mac, 1, 24, 1);
    assert(iface != NULL);
    void interface_free(Interface *arg0);
    Packet * packet_create(size_t capacity);
    Packet *req_pkt = packet_create(64);
    assert(req_pkt != NULL);
    void packet_free(Packet *arg0);
    uint8_t req_pkt_data[64];
    memset(req_pkt_data, 0, sizeof(req_pkt_data));
    if (req_pkt->data == NULL) req_pkt->data = req_pkt_data;
    if (req_pkt->len == 0) req_pkt->len = 8;
    memset(req_pkt->data, 0, req_pkt->len);
    req_pkt->len = 28;
    assert(sim != NULL);
    assert(iface != NULL);
    assert(req_pkt != NULL);
    uint8_t req_pkt_data_buffer[64];
    memset(req_pkt_data_buffer, 0, sizeof(req_pkt_data_buffer));
    if (req_pkt->data == NULL) req_pkt->data = req_pkt_data_buffer;
    memset(req_pkt->data, 0, req_pkt->len);
    int out_ret = arp_send_reply(sim, iface, req_pkt);
    assert(out_ret == -1); /* EVA-proven oracle */
    simulator_free(sim);
    printf("PASS test_send_reply_ir_if_0_false_false_not_sim_and_false_not_iface_and_false_not_req_pkt_tv001\n");
}

static void test_send_reply_ir_if_1_left_not_req_pkt_data_tv001(void) {
    Simulator * simulator_create(Topology * topo, Scheduler * sched);
    Topology * topology_create(void);
    Topology *sim_topo = topology_create();
    assert(sim_topo != NULL);
    void topology_free(Topology *arg0);
    Scheduler * scheduler_create(size_t capacity);
    Scheduler *sim_sched = scheduler_create(64);
    assert(sim_sched != NULL);
    void scheduler_free(Scheduler *arg0);
    Simulator *sim = simulator_create(sim_topo, sim_sched);
    assert(sim != NULL);
    void simulator_free(Simulator *arg0);
    Interface * interface_create(const char * name, const uint8_t * mac, uint32_t ip_addr, uint8_t prefix_len, uint16_t mtu);
    uint8_t iface_mac_buf[64];
    memset(iface_mac_buf, 0, sizeof(iface_mac_buf));
    const uint8_t * iface_mac = iface_mac_buf;
    Interface *iface = interface_create("kleva", iface_mac, 1, 24, 1);
    assert(iface != NULL);
    void interface_free(Interface *arg0);
    Packet * packet_create(size_t capacity);
    Packet *req_pkt = packet_create(64);
    assert(req_pkt != NULL);
    void packet_free(Packet *arg0);
    uint8_t req_pkt_data[64];
    memset(req_pkt_data, 0, sizeof(req_pkt_data));
    if (req_pkt->data == NULL) req_pkt->data = req_pkt_data;
    if (req_pkt->len == 0) req_pkt->len = 8;
    memset(req_pkt->data, 0, req_pkt->len);
    req_pkt->len = 28;
    assert(sim != NULL);
    assert(iface != NULL);
    assert(req_pkt != NULL);
    (req_pkt)->data = 0;
    uint8_t req_pkt_data_buffer[64];
    memset(req_pkt_data_buffer, 0, sizeof(req_pkt_data_buffer));
    if (req_pkt->data == NULL) req_pkt->data = req_pkt_data_buffer;
    memset(req_pkt->data, 0, req_pkt->len);
    int out_ret = arp_send_reply(sim, iface, req_pkt);
    assert(out_ret == -1); /* EVA-proven oracle */
    simulator_free(sim);
    interface_free(iface);
    printf("PASS test_send_reply_ir_if_1_left_not_req_pkt_data_tv001\n");
}

int main(void) {
    test_init_null_tv001();
    test_init_valid_tv001();
    test_init_ir_if_0_not_sim_tv001();
    test_init_ir_if_0_false_not_sim_tv001();
    test_send_request_null_tv001();
    test_send_request_valid_tv001();
    test_send_request_ir_if_0_left_left_not_sim_tv001();
    test_send_request_ir_if_0_left_right_not_iface_tv001();
    test_send_request_ir_if_0_right_not_target_ip_tv001();
    test_send_request_ir_if_0_false_false_not_sim_and_false_not_iface_and_false_not_target_ip_tv001();
    test_send_request_ir_diversity_target_ip_one_tv001();
    test_send_reply_null_tv001();
    test_send_reply_valid_tv001();
    test_send_reply_ir_if_0_left_left_not_sim_tv001();
    test_send_reply_ir_if_0_left_right_not_iface_tv001();
    test_send_reply_ir_if_0_false_false_not_sim_and_false_not_iface_and_false_not_req_pkt_tv001();
    test_send_reply_ir_if_1_left_not_req_pkt_data_tv001();

    printf("\nAll unit tests passed.\n");
    return 0;
}
